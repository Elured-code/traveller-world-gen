[pytest]
# Discover tests in the tests/ subdirectory
testpaths = tests

# Show short tracebacks and extra test summary info for failures
addopts = -v --tb=short -q

# Treat warnings as informational only (Azure Functions SDK may emit some)
filterwarnings =
    ignore::DeprecationWarning
