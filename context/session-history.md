# session-history.md — Session history

Append one row per session. Never delete entries — they explain why the code is
shaped the way it is.

| Date | Summary |
|------|---------|
| 2026-04-14 | Session 1 — CRB mainworld generation, UWP, trade codes, JSON Schema, HTML card, Azure Functions (5 endpoints), test suite |
| 2026-04-17 | Session 2 — WBH stellar generation, orbit placement, system integration, orbital temperature, three orbit compliance bug fixes |
| 2026-04-18 | Session 3 — Secondary world SAH/social detail, moon generation quantity/sizing, moon SAH/social detail, compliance audit |
| 2026-04-19 | Session 4 — Belt population and mainworld fixes, moon size clamping, JSON serialisation of all detail data, `summary()` updates, four new system API endpoints, REST API documentation, GitHub prep |
| 2026-04-22 | Session 5 — Pylance fixes across all modules; orbit spread inner-system clustering bug fixed (min_spread floor + additive gap check); security hardening (`parse_hex_pos`, `ERR_INVALID_HEX`, `max_batch_size` bounds, upstream error scrubbing, CORS note); documentation updates; session context file created |
| 2026-04-23 | Session 6 — Created CLAUDE.md consolidating all .claude/context/ files; updated README generation rules section with full WBH coverage; refreshed backup archive |
| 2026-04-23 | Session 7 — Added `World.from_dict()` classmethod; added `generate_system_from_world()` entry point; added `parse_world_json()` helper; added `POST /api/system/from-world` endpoint (13th endpoint); created GTK4 UI skeleton in `gen-ui/app.py` |
| 2026-04-24 | Session 8 — GTK4 UI fully implemented: mainworld generation, HTML card auto-opened in system browser via `Gio.AppInfo.launch_default_for_uri`, save to JSON/text/HTML via `Gtk.FileDialog`; added `gen-ui/README.md` and `gen-ui/requirements.txt` |
| 2026-04-24 | Session 9 — Randomised seeding when no seed specified (`secrets.randbelow`) across API, generation modules, and gen-ui; `seed` field added to all JSON responses and JSON Schema; HTML hydrographics right-alignment fix; gen-ui system generation controls added |
| 2026-04-27 | Session 10 — gen-ui TravellerMap lookup wired (`generate_system_from_map`); `AmbiguousWorldError` added to `traveller_map_fetch.py`; disambiguation dialog in gen-ui (GitHub issues #1, #2 closed) |
| 2026-04-27 | Session 11 — gen-ui replaced browser-auto-open with native GTK4 in-app display; world card shows Physical/Society detail cards, stat boxes, trade code badges, notes; Full System checkbox wired to `generate_full_system()` |
| 2026-04-27 | Session 12 — Temperature compliance bug fixed: sub-Orbit#1 deviation scaling removed from `hz_deviation_to_raw_roll()` and `_temp_zone()`; pre-fix 58% frozen-in-HZ for dim stars → post-fix 0%; 502 tests pass |
| 2026-04-27 | Session 13 — Gas giant mainworld compliance bug fixed: `_gg_sah_roll()` inlined in `traveller_orbit_gen.py`, stored in `OrbitSlot.gg_sah`; gas giant branch added to `generate_mainworld_at_orbit()`; 10 new tests; 512 tests pass |
| 2026-04-29 | Session 14 — Created `system_map.py`: SVG star system map with per-star arc zones, log-AU radial scale, companion-star dashed arcs, orbit table zone; SVG-only output |
| 2026-04-30 | Session 15 — Ag trade code compliance fix (issue #4): corrected all three criteria, added population 5-7 check, six boundary tests. Added white background theme to system maps: `ColourPalette` dataclass, `PALETTE_DARK`/`PALETTE_LIGHT`, `--white-bg` flag. 523 tests pass. |
| 2026-04-30 | Session 16 — pip-installed `pycairo` and `PyGObject` into `.venv-1` (compiled against Homebrew GTK4 libs); added macOS DYLD re-exec block to `gen-ui/app.py` |
| 2026-04-30 | Session 17 — Migrated `gen-ui/app.py` from GTK4/PyGObject to PySide6 (Qt6): complete rewrite (~1100 lines); all generation logic unchanged; `QKeySequence::Quit/Close` handle cross-platform shortcuts automatically |
| 2026-04-30 | Session 18 — gen-ui bug fixes and layout polish: fixed `_on_source_toggled` early-return guard; fixed macOS SSL cert failure for TravellerMap; widened detail row value column; restructured source row layout |
| 2026-04-30 | Session 19 — Three Pylance fixes in `gen-ui/app.py`: `QApplication.instance()` narrowed via `isinstance`; `_clear_status` guards `takeAt()`/`widget()` against `None`; `orbit.world_type` cast to `str` via `str()` |
| 2026-04-30 | Session 20 — Pylance fixes in `traveller_orbit_gen.py` and `traveller_world_detail.py`: `OrbitSlot.detail` promoted to typed field; all `getattr` workarounds removed. Upgraded pip/setuptools (resolved 7 CVEs). Created `.github/workflows/dependency-audit.yml`. Updated `docs/developer-guide.md`. 523 tests pass. |
| 2026-04-30 | Session 21 — gen-ui `SystemMapWindow`: separate non-modal `QMainWindow` with `QSvgWidget`-rendered SVG, dark/light theme toggle, Save SVG button; "System Map" button added to system result header; SVG background fixed as explicit `<rect fill>` element; table zone wrapped in `<g font-family>` for Qt monospace. 523 tests pass. |
| 2026-05-05 | Session 22 — Pylint 10.00/10 pass across all six generation modules: docstrings, elif→flat-if, inline expansions, unused param rename, chained comparison fix, superfluous parens removed, `_size_gg_moon()` indentation bug fixed. 523 tests pass. |
| 2026-05-06 | Session 23 — New module `traveller_world_physical.py`: composition, density, diameter, mass/gravity/escape velocity, axial tilt (6-band table), basic rotation rate; `WorldPhysical` dataclass with `to_dict()`; `generate_world_physical(world, age_gyr)`. Updated `World.physical` field, `to_dict()`/`to_html()`/`summary()`. gen-ui: "Physical detail" checkbox, `_build_physical_card()`, stellar card shows system age. GitHub issues #10, #11 filed. 523 tests pass. |
| 2026-05-07 | Session 24 — Gas giant mainworld orbit display bug fixed (issue #22): three-layer fix across `attach_detail()`, `_orbit_profile()`, and `to_html()`; satellite appears as `moons[0]` sub-row; gas giant profile shown in orbit table. 523 tests pass. |
| 2026-05-07 | Session 25 — Implemented WBH p.77 Extreme Axial Tilt sub-table (issue #10): replaced placeholder in `_roll_extreme_axial_tilt()` with full 6-band 1D table producing 20–70° (high), 40–90° (extreme), 91–126° (retrograde), 144–179° (extreme retrograde), 130–180° (extreme retrograde high-variance). Updated module docstring. Deferred feature entry removed. 523 tests pass. |
| 2026-05-07 | Session 26 — Restructured AI context: CLAUDE.md rewritten as a short router (~70 lines); `context/` directory created with 13 topic files (common, generation-pipeline, data-structures, stellar-orbit, system-world, detail-moon, map-fetch, api-layer, system-map, gen-ui, compliance-audit, deferred-features, session-history). Content consolidated from CLAUDE.md and developer-guide.md; gaps fixed (OrbitSlot.gg_sah, WorldDetail.trade_codes, WorldPhysical added to data-structures). |
| 2026-05-08 | Session 27 — Tidal lock status implemented (WBH pp. 105–107, issue #11): 2D+DM table with 11 outcomes (no-effect, braking ×1.5–×5, prograde, retrograde, 3:2 lock, 1:1 lock); broken-lock check; axial tilt reroll for lock results; `WorldPhysical.tidal_status` field added; `generate_world_physical()` extended with `orbit_number`, `orbit_au`, `star_mass` optional params; gen-ui call site updated; `TIDAL_STATUS_LABELS` exported for display in `to_html()`/`summary()`/gen-ui card. 42 new tests; 565 tests pass. |
| 2026-05-08 | Session 28 — Belt physical detail implemented (WBH pp. 131–133, issue #19): new module `traveller_belt_physical.py` with `BeltPhysical` dataclass and `generate_belt_physical()`; `WorldDetail.physical` field added (`BeltPhysical | None`); `generate_system_detail()` computes span, composition, bulk, resource rating, and significant bodies for every belt orbit slot; `WorldDetail.to_dict()` includes `"physical"` key; 45 new tests; 610 tests pass. |
| 2026-05-08 | Session 29 — Belt physical detail for mainworld belts (issue #19 cont.): `attach_detail()` extended to generate `BeltPhysical` for belt mainworlds and set both `mainworld.physical` and `mainworld_orbit.detail.physical`; `World.physical` type widened to `Union[WorldPhysical, BeltPhysical]`; `World.to_html()` and `summary()` render belt card via `isinstance` dispatch; `TravellerSystem.to_html()` mainworld panel gains a belt body block; gen-ui `_build_physical_card()` dispatches on `isinstance(physical, BeltPhysical)`; 4 new tests; 614 tests pass. |
| 2026-05-08 | Session 30 — Two belt display bug fixes: (1) gen-ui `_build_stat_row()` accessed `physical.diameter_km`/`gravity` without BeltPhysical guard, crashing the system card and leaving only the header visible — fixed with `not isinstance(physical, BeltPhysical)` condition; (2) belt span formula was using AU range of system (`max_au − min_au`) instead of the WBH orbit generation spread `(max Orbit# − MAO) / n_orbits` — all belts in all systems were getting wildly large spans (e.g., 0–11.974 AU for a belt at 0.348 AU); corrected in both `generate_system_detail()` and `attach_detail()` mainworld block; issue #27 filed and closed. 614 tests pass. |
| 2026-05-11 | Session 31 — WBH atmosphere detail Phase 1 (pp. 78–82): new `AtmosphereDetail` dataclass on `World` with `pressure_bar`, `oxygen_partial_pressure`, `scale_height_km`. Helpers `_atmosphere_pressure_bar()`, `_oxygen_partial_pressure()`, `_scale_height_km()` in `traveller_world_gen.py`; `ATMOSPHERE_PRESSURE_SPAN_BAR` table for codes 1–9, D, E; `SIZE_GRAVITY_G` exported for the 8.5/g scale-height approximation; `_dice()` unclamped helper for the (2D-7)/100 variance term. New `generate_atmosphere_detail()` and `format_atmosphere_profile()` produce a WBH p.82 profile string. `World._atmosphere_dict()` adds nested `detail` and `profile` keys to JSON output when populated. Orchestrator (`generate_mainworld_at_orbit()`) now takes `system_age_gyr` and calls the detail generator after the atmosphere roll. Schema updated. 52 new tests; 640 tests pass. |
| 2026-05-12 | Session 32 — WBH atmosphere detail Phase 2 Stage 1 (pp. 82–85): taint generation for tainted atmosphere codes (2, 4, 7, 9). New `Taint` dataclass; constants `_TAINTED_CODES`, `_O2_TAINT_CODES`, `_BIOLOGIC_SUBTYPE_ROLLS`, `_TAINT_SUBTYPE_DM`, `_TAINT_SUBTYPE_TABLE`, `_TAINT_SEVERITY_TABLE`, `_TAINT_PERSISTENCE_TABLE`; helpers `_taint_severity_code()`, `_taint_persistence_code()`, `_roll_single_taint()`. `AtmosphereDetail` gains `taints: list`; `to_dict()` emits `"taints"` key when non-empty. Cascade mechanic: result 10 on subtype table (Particulates) triggers a second taint roll. `generate_atmosphere_detail()` gains reserved `temperature` parameter. Schema updated. 42 new tests; 682 tests pass. Also: `World.physical` renamed to `World.size_detail` throughout; JSON key `"physical"` → `"size_detail"`. Schema updated. Atmosphere detail coupling: gen-ui now ensures `atmosphere_detail` is set whenever size detail is generated. |
| 2026-05-12 | Session 32 cont. — Phase 2 Stage 2: `format_atmosphere_profile()` extended with `T.S.P` taint suffix (subtype code, severity code, persistence code per taint, e.g. `7-1.148-0.138-P.7.9`). gen-ui atmosphere card now displays each taint (subtype, severity, persistence). 8 new tests; 690 tests pass. |
| 2026-05-12 | Session 32 cont. — Phase 2 Stage 3: `World.to_html()` gains an Atmosphere detail inner card (profile, pressure, O₂, scale height, taints with severity/persistence); `World.summary()` gains an atmosphere detail section. Issue #29 closed. 18 new tests; 708 tests pass. |
| 2026-05-12 | Session 33 — Phase 3 Stage 1 (pipeline reorder): in `generate_mainworld_at_orbit()` both the gas-giant and terrestrial branches now call `generate_temperature_from_orbit()` before `generate_atmosphere_detail()`, passing `world.temperature` as the `temperature` parameter. `generate_system_from_world()` already had the correct order. Seed-breaking change (accepted). 708 tests pass. |
| 2026-05-12 | Session 33 cont. — Phase 3 Stage 2: exotic/corrosive/insidious atmosphere subtypes and insidious hazards (WBH pp.85-87). New `InsidiousHazard` dataclass; `_EXOTIC_SUBTYPE_TABLE`, `_CI_SUBTYPE_TABLE`, `_INSIDIOUS_HAZARD_TABLE`, `_HAZARDOUS_GASES` constants; `_roll_exotic_subtype()`, `_roll_ci_subtype()`, `_roll_insidious_hazard()`, `_subtype_pressure_bar()` helpers. `AtmosphereDetail` gains `subtype_code`, `subtype_name`, `hazards` fields. `hz_deviation` parameter added to `generate_atmosphere_detail()` and all call sites. Unbound pressure (subtype C/D/E) stored as `None`; displayed as "> 10.0 bar". `World.to_html()`, `summary()`, gen-ui card updated to show subtype and hazards. Schema updated. 33 new tests; 743 tests pass. |
| 2026-05-12 | Session 34 — Phase 4: Gas mix generation (WBH pp.87–95). New `GasMixComponent` dataclass; `_GAS_CODES` dict (24 gases); 7 gas-mix table constants (Boiling-VH, Boiling-H, Hot, Temperate, Cold, Frozen-M, Frozen-D); helpers `_select_gas_mix_table()`, `_roll_single_gas()`, `_roll_gas_mix()`; public `generate_gas_mix()` mutates `AtmosphereDetail.gas_mix` after hydrographics. CO* substitution: CO→CO₂ (non-frozen, hydro>0) or CO→N₂ (frozen, hydro>0). `format_atmosphere_profile()` appends `:code-##` gas tokens. `to_html()`, `summary()`, gen-ui atmosphere card show gas mix. Three call sites added to `traveller_system_gen.py` (after hydrographics). Schema updated. GitHub issue #44 filed (atmospheric retention check). 62 new tests; 805 tests pass. |
| 2026-05-13 | Session 34 cont. — gen-ui atmosphere detail call-site fixes: imported `generate_gas_mix`; added `temperature=world.temperature` to both `generate_atmosphere_detail` call sites in `_finish_generation` and `_finish_system_generation`; added `generate_gas_mix` calls after each (inside the `atmosphere_detail is None` guard so pipeline-generated worlds are not re-rolled). Standalone worlds now populate gas mix and use temperature for Phase 3 subtype DMs. 805 tests pass. |
| 2026-05-13 | Session 34 cont. — gen-ui atmosphere detail always visible: decoupled `atmosphere_detail` generation from the Physical detail checkbox in `_finish_generation`. Atmosphere detail (Phases 1–4) is now generated unconditionally for standalone worlds; only the World Body card (`size_detail`) remains gated on the Physical checkbox. TravellerMap and full-system paths unaffected (pipeline sets `atmosphere_detail` before `_finish_generation` runs). 805 tests pass. |
| 2026-05-13 | Session 35 — Phase 5: altitude bands and Unusual (F) atmosphere subtypes (WBH pp.90–93). Stage 1: `_compute_very_dense_altitude()` and `_compute_low_altitude()` helpers using Bad Ratio formula; `AtmosphereDetail` gains `min_safe_altitude_km` and `no_safe_altitude` fields; optional taint roll (1D≥4) for codes 13/14 added to `generate_atmosphere_detail()`. Stage 2: `UnusualSubtype` dataclass; `_UNUSUAL_SUBTYPE_TABLE` (D26, 12 subtypes); `_d26()`, `_roll_unusual_subtype()` helpers with Layered/Panthalassic/Steam prerequisite checks; public `generate_unusual_subtype()` mutates `AtmosphereDetail.unusual_subtypes`; `format_atmosphere_profile()` short-circuit for code 15 (`F-S{codes}`); `AtmosphereDetail` gains `unusual_subtypes` field. Stage 3: `to_html()`, `summary()`, and gen-ui atmosphere card show altitude and unusual subtype rows. Three call sites in `traveller_system_gen.py`, four + batch in `function_app.py`, two in `gen-ui/app.py`. Schema updated with three new properties. `import math` added. ~58 new tests; 890 tests pass. |
| 2026-05-13 | Session 36 — Bug fixes (issue #51): (1) `TravellerSystem.to_html()` mainworld panel now renders `WorldPhysical` and atmosphere detail inner-cards (was omitting both; only `BeltPhysical` was handled). Added `.inner-card`, `.inner-lbl`, `.drow`, `.dlbl` CSS and a `drow()` helper to the system HTML template; imports `WorldPhysical`, `TIDAL_STATUS_LABELS`, `format_atmosphere_profile`. (2) `generate_system_from_map()` in `traveller_map_fetch.py` now calls `generate_gas_mix()` and `generate_unusual_subtype()` after `generate_atmosphere_detail()`, completing the atmosphere pipeline for TravellerMap-fetched worlds; `hz_deviation` now threaded into `generate_atmosphere_detail()` for orbit-position DMs. 890 tests pass; pylint 10.00/10 on both modified files. |
| 2026-05-15 | Session 37 — Install scripts: `install.bat` committed; `install.ps1` and `install.sh` gained Python auto-install (winget on Windows; brew/apt/dnf/pacman on macOS/Linux). New module `traveller_hydro_detail.py`: `HydrographicDetail` dataclass, `generate_hydrographic_detail()`, WBH p.93 surface liquid percentage (flat random over code ranges); wired into `traveller_system_gen.py` (shared section), `function_app.py` (4 handlers + batch), `traveller_map_fetch.py`, and both gen-ui `_finish_generation` paths. New `_build_hydrographic_card()` in gen-ui. Hydrographic Detail inner-card added to `World.to_html()`, `TravellerSystem.to_html()`, and `World.summary()`. JSON: `hydrographics.detail.surface_liquid_pct` nested under `"detail"` key. Schema updated. `# pylint: disable=too-many-lines` added to `traveller_system_gen.py`. `test_function_app.py` updated for pipeline seed shift. 29 new tests in `tests/test_hydro_detail.py`; 919 tests pass. |
| 2026-05-15 | Session 37 cont. — Bug fix #55 (H/L oxygen taint ppo validation): `_roll_single_taint()` gained `ppo: Optional[float]` parameter; High Oxygen (H) now rerolled unless ppo > 0.5 bar; Low Oxygen (L) now rerolled unless ppo < 0.1 bar. ppo computation moved before the taint block in `generate_atmosphere_detail()` so it is available at taint time (seed-breaking for tainted codes 2, 4, 7, 9, 13, 14). Fixed one hanging test (`test_taint_fires_for_code_14` — infinite loop from H taint blocked by the new guard). Two existing tests updated to pass ppo; 6 new tests in `TestTaintPpoValidation`. 925 tests pass. |
| 2026-05-15 | Session 38 — NHZ atmosphere generation (WBH pp. 78–79). New atmosphere codes 16 (Gas, Helium / G) and 17 (Gas, Hydrogen / H) added to `ATMOSPHERE_NAMES`, `ATMOSPHERE_GEAR`, `TEMPERATURE_DM`. Four NHZ table constants: `_NHZ_HOT_A` (HZCO ≤ -2.01), `_NHZ_HOT_B` (-1.01 to -2.0), `_NHZ_COLD_A` (+1.01 to +3.0), `_NHZ_COLD_B` (≥ +3.01); each maps 2D-7+Size results to `(atm_code, base_exotic_key, irritant_exotic_key, star, dagger)` tuples. New `generate_nhz_atmosphere(size, hz_deviation)` returns `(atm_code, exotic_key)`. `generate_atmosphere_detail()` gains `exotic_key_override` parameter (bypasses `_roll_exotic_subtype()` for NHZ exotic worlds) and early-return for codes 16/17. `generate_hydrographics()` returns 0 for codes 16/17. `format_atmosphere_profile()` returns `to_hex(code)` for codes 16/17. `generate_full_system()` and `generate_mainworld_at_orbit()` gain `nhz_atmospheres: bool = False`; NHZ branch fires when `abs(hz_deviation) > 1.0`. CLI gains `--nhz-atmospheres` flag. gen-ui: "NHZ Atmospheres" checkbox added (enabled only when System detail is checked). JSON schema `atmosphere.code.maximum` updated 15→17. 33 new tests; 952 tests pass. |
| 2026-05-15 | Session 38 cont. — NHZ atmospheres extended to secondary worlds and moons. `TravellerSystem` gains `nhz_atmospheres: bool = False` field (set by `generate_full_system()`). `_terrestrial_sah()`, `_moon_detail()`, and `generate_system_detail()` each gain `nhz_atmospheres: bool = False` parameter. `attach_detail()` reads `system.nhz_atmospheres` and threads it through all secondary-world and moon generation paths. `generate_nhz_atmosphere` added to `traveller_world_detail.py` imports. No changes to `gen-ui/app.py` or `function_app.py` — flag travels via `system.nhz_atmospheres`. 5 new tests in `TestNhzSecondaryWorlds`; 957 tests pass. |
| 2026-05-18 | Session 39 — Companion star exclusion zone bug fixed in `traveller_orbit_gen.py`. When companion orbit# < 1.0, `excl = companion_orbit - 1.0` was ≤ 0 and never triggered the `max_o` cap, allowing primary worlds inside the WBH exclusion zone [companion−1, companion+3]. Fix: `else` branch pushes `mao = max(mao, companion_orbit + 3.0)` and syncs `star_mao[designation]`. Also: `system_map.py` extended to render companion star rows inside the primary star's orbit table section, sorted by orbit number. 2 new tests in `TestCompanionExclusionZone`; 959 tests pass. |
| 2026-05-18 | Session 39 cont. — Primary star outer zone placement implemented in `traveller_orbit_gen.py`. Primary stars with a valid inner zone (companion−1 > MAO) now also populate the outer zone [companion+3, 17]. New `star_outer` dict tracks outer zone bounds; `_avail_range()` nested helper includes outer range in proportional world allocation; placement loop wraps in a `for zone in zones` loop that runs once per zone (inner + outer). 4 new tests in `TestPrimaryOuterZone`; 963 tests pass. |
| 2026-05-18 | Session 39 cont. — `system_map.py` orbit table gains a column label sub-header row (`#  Orbit#  AU  Type  Profile  Codes  Zone ♦`). `Zone ♦` makes explicit that the last column shows both temperature zone and moon count. `_TBL_ROW0_OFF` bumped 38→50 to accommodate the new row. |
| 2026-05-18 | Session 40 — Companion star orbit periods implemented. `Star` gains `orbit_period_yr: Optional[float]` field; computed in `generate_stellar_data()` using `√(AU³/(M+m))`. M_central for companions = parent mass; for secondaries = combined mass of all stars with effective system orbit# < this star's orbit#. `_eff_sysorn()` nested helper handles companions-of-secondaries correctly (e.g. Ba's effective system orbit = B's orbit#, so Ca's M includes Ba). `Star.to_dict()` and `StarSystem.summary()` include the period. `system_map.py`: new `_C_PERIOD = 490` column, `_fmt_period()` helper (h/d/y auto-scaling), period shown in companion/secondary star rows and non-primary column headers. No new dice rolls; no seed disruption. 963 tests pass. |
| 2026-05-18 | Session 41 — Anomalous orbits implemented (WBH pp.49-50, issue #12). `OrbitSlot` gains `anomaly_type: str` field. `generate_orbits()` now rolls for anomalous orbit count (2D: ≤9 → 0, 10 → 1, 11 → 2, 12 → 3) and type (random, eccentric, inclined, retrograde, trojan). Random/eccentric/inclined/retrograde use a 2D-2 + d10 fractional roll constrained to the star's valid zone (respecting companion exclusion bands with ±0.01 clearance from boundaries). Trojan orbits pick an existing non-empty host slot and co-occupy its orbit#. Inclined orbits roll (1D+2)×10 + d10 degrees. `OrbitSlot.to_dict()` emits `anomaly_type` when set. `system_map.py`: anomaly indicator appended to type column (e.g., "terr R" for retrograde, "terr L4" for trojan_leading). `TravellerSystem.to_html()` and `SystemOrbits.summary()` show anomaly notes in the notes column. Seed-breaking for all systems (new 2D roll fires every generation). 6 new tests in `TestAnomalousOrbits`; 969 tests pass. |
| 2026-05-18 | Session 40 cont. — World orbital periods implemented. `OrbitSlot` gains `orbit_period_yr: Optional[float]` (`init=False`); computed in `generate_orbits()` after slot placement using `√(AU³/M_central)`. M_central = designated star mass + companions whose orbit_au < world orbit_au. Planet mass correction (WBH: mE×0.000003) omitted as negligible. `OrbitSlot.to_dict()` emits `orbit_period_yr`. `system_map.py` Period column now populated for world rows. `gen-ui/app.py`: `_fmt_period()` promoted to module level; System Orbits card gains Period column in both detail/non-detail variants. No new dice rolls; 963 tests pass. |
| 2026-05-18 | Session 41 cont. — Notes column added to gen-ui System Orbits card and `TravellerSystem.to_html()` orbit table. `_build_orbits_card()` now has 10 columns (detail_attached) and 8 columns (non-attached), both with a trailing Notes column. `to_html()` orbit table now uses a `note_parts` list combining the "← mainworld" marker with `OrbitSlot.notes`, showing all notes unconditionally (anomaly notes, HZ notes, etc.). No new tests; 969 tests pass. |
| 2026-05-18 | Session 42 — Bug fix #52 (incorrect belt counts for fetched mainworlds). Two bugs in `traveller_map_fetch.py`: (1) `_reconcile_orbit_types()` now promotes empty orbit slots when canonical_gg + canonical_belt exceeds available non-empty non-mainworld slots, so belt/GG counts are always honoured. Previously belts were silently dropped because the pool was truncated before shuffle. (2) `generate_system_from_map()` now subtracts 1 from `canonical_belt` when the mainworld is a belt (size==0), because WBH convention (confirmed in `generate_belt_count()`) includes the mainworld belt in the PBG B digit. 4 new tests in `TestReconcileOrbitTypes`; 973 tests pass. |
| 2026-05-19 | Session 46 — Orbital inclination implemented (WBH p.28, issue #59). Optional feature gated on `orbital_inclination=False` flag (same pattern as `orbital_eccentricity`). `Star` gains `orbit_inclination: float = 0.0`; `OrbitSlot` gains `inclination: float = field(default=0.0, init=False)`. `_roll_inclination()` added to `traveller_orbit_gen.py` using 6-row severity table (Very Low/Low/Moderate/High/Very High/Extreme) with recursive retrograde case (2D=12 → 180 − sub-roll). Post-placement pass in `generate_orbits()` sets inclination for all non-empty, non-anomalous-inclined slots when flag is set; anomalous "inclined" orbits skipped (angle already in notes). `to_dict()` emits `inclination` when > 0. `TravellerSystem` stores `orbital_inclination` flag. Flag plumbed through `generate_full_system()`, `generate_system_from_world()`, and all 5 system API endpoints via new `parse_orbital_inclination()` helper. gen-ui and HTML orbit table: "Ecc" column renamed "Ecc/Incl"; cell shows `ecc/incl°` combined, falling back to "—" when both zero. gen-ui gains "Orbital Inclination" checkbox (enabled only when "Full system" is checked). No seed disruption when flag is False. 8 new tests in new `tests/test_orbit_gen.py`; 998 tests pass. |
| 2026-05-19 | Session 45 — Three maintenance fixes. (1) JSON Unicode error in tests (issue #60): `open(schema_path)` in `tests/test_traveller_world_gen.py` lacked `encoding="utf-8"`, causing failures on Windows where the default codepage (cp1252) cannot decode non-ASCII characters in `traveller_world_schema.json`. Fixed to `open(schema_path, encoding="utf-8")`. (2) Virtual environment consolidated (issues #61 and #62): dual `.venv` + `.venv-1` setup replaced by a single `.venv`; install scripts (`install.sh`, `install.ps1`, `install.bat`) now install all dependencies in one pass from `requirements.txt`, `gen-ui/requirements.txt`, and new `requirements-dev.txt` (pytest/pylint), then activate the venv. All `.vscode/settings.json`, docs, and gen-ui references updated from `.venv-1` to `.venv`. |
| 2026-05-18 | Session 44 cont. — Dedicated `Ecc` column added to gen-ui System Orbits card and `TravellerSystem.to_html()` orbit table (replaces the former inline `(e=X.XXX)` embedded in the AU cell). Both header variants gain a right-aligned `"Ecc"` column inserted after `"AU"`: detail_attached is now 11 columns (`right_cols={1,2,3,9}`); non-attached is 9 columns (`right_cols={1,2,3,7}`). AU cells now show bare `orbit_au:.3f`. Moon sub-row `colspan` in `to_html()` widened from 3 → 4. Separate fix: added `_check_ecc` ("Orbital Eccentricity") checkbox to gen-ui, enabled/disabled alongside "NHZ Atmospheres" when "System detail" is toggled; passes `orbital_eccentricity=self._check_ecc.isChecked()` to `generate_full_system()`. Previously the flag defaulted to False on every call, so all `OrbitSlot.eccentricity` values were 0.0 and the Ecc column always showed "—". |
| 2026-05-18 | Session 44 — 1:1 tidal lock axial tilt and eccentricity interactions implemented (WBH p.77, issue #10). Two compliance bugs fixed in `traveller_world_physical.py`: (1) Rule 3: `_apply_tidal_lock_result()` 1:1 case now calls new `_roll_axial_tilt_1d()` unconditionally (1D selects outer band on Axial Tilt table, inner 1D gives degrees within band) instead of the incorrect `_reroll_axial_tilt_for_lock()` + `> 3.0` guard. (2) Rule 4: `_reroll_eccentricity_tidal()` rolls eccentricity with DM-2 for 1:1 locked worlds; `generate_world_physical()` gains `orbit_eccentricity: float = 0.0` parameter; when `tidal_status == "1:1_lock"` and `orbit_eccentricity > 0.1`, the lower of original/new eccentricity is stored in new `WorldPhysical.eccentricity_adjusted` field. `function_app._attach_mainworld_physical()` threads the orbit slot eccentricity in and propagates any reduction back to the orbit slot. `_ECC_TABLE_PHYS` inlined to avoid circular import. 3:2 lock axial tilt unchanged. 11 new tests; 990 tests pass. |
| 2026-05-19 | Session 48 — Bug fix #64: anomalous orbit eccentricity DMs not applied (WBH pp.49-50). Random/eccentric/inclined/retrograde anomalous orbits must apply DMs +2/+5/+2/+2 when rolling on the Eccentricity Values table; trojan types have no DM. Added `_ANOM_ECC_DM` dict after `_ECC_TABLE`; added `anomaly_dm: int = 0` parameter to `_roll_eccentricity()` (+ pylint suppress `too-many-arguments,too-many-positional-arguments`); eccentricity pass in `generate_orbits()` now passes `anomaly_dm=_ANOM_ECC_DM.get(o.anomaly_type, 0)`. No seed disruption for systems without anomalous orbits. 6 new tests in `TestAnomalyEccentricityDMs`; 1008 tests pass. |
| 2026-05-19 | Session 50 — Tidal lock eccentricity DM implemented (WBH p.105). `_tidal_lock_dm()` gains `orbit_eccentricity: float = 0.0` parameter; when `e > 0.1` applies `DM − floor(e × 10)` (general DM, all cases). Threaded through `_roll_tidal_lock_status()` and `generate_world_physical()` (already had `orbit_eccentricity` parameter from Session 44). No seed disruption when `orbital_eccentricity=False` (default). Stale "Orbital eccentricity" row removed from `context/deferred-features.md` (feature fully implemented in Sessions 43 + 48). 6 new tests in `TestTidalLockEccentricityDm`; 1014 tests pass. |
| 2026-05-19 | Session 52 — Moon quantity adjacency DMs implemented (WBH p.56, issue #14). Three previously deferred DM conditions now applied in `_moon_quantity()`: companion star exclusion zones (orbit# ±1 to +3), host star MAO boundary (±1.0), outermost Far-star slot proximity (±1.0). New `_moon_adjacency_context()` helper in `traveller_world_detail.py` computes context from system structure. `generate_moons()` and `_moons_for()` gain three new optional params; both `generate_system_detail()` and `attach_detail()` call sites pass context. 8 new tests in `TestMoonQuantityAdjacencyDMs`; 1044 tests pass. |
| 2026-05-19 | Session 52 cont. — Moon tidal lock DMs and planet-to-moon lock implemented (WBH pp.106–107, issue #11). `_tidal_lock_dm()` gains moon-size DM (DM − total size of significant moons) and multi-star DM (DM − stars orbited, defaulting to 1). New `_planet_moon_lock_dm(moon, all_moons)` implements WBH p.107 DM table (base −10, size DM, PD-range DMs, multiple-moons penalty). `_roll_tidal_lock_status()` now assembles star + moon candidates sorted by highest DM (moon before star on tie) and cascades until a lock fires. New public `apply_moon_tidal_effects()` resolves circular dependency (WorldPhysical needs moons; moons need WorldPhysical) via three-phase pipeline: `generate_world_physical()` → moon gen → `apply_moon_tidal_effects()`. `function_app.py`: `_get_mainworld_moons()` + `_apply_mainworld_moon_tidal()` helpers wired into 6 call sites. `gen-ui/app.py`: inline moon extraction and call in `_finish_system_generation()`. 24 new tests across 4 new test classes; 1068 tests pass. |
| 2026-05-19 | Session 51 — Moon orbit placement implemented (WBH pp.74–77, issue #16). `traveller_moon_gen.py`: `Moon` dataclass gains 8 new fields (`orbit_pd`, `orbit_km`, `orbit_range`, `orbit_period_hours`, `ring_centre_pd`, `ring_span_pd`, `orbit_eccentricity`, `orbit_retrograde`). New helpers: `_hill_sphere_au`, `_hill_sphere_pd`, `_hill_moon_limit`, `_moon_orbit_range`, `_roll_moon_pd`, `_moon_period_hours`, `_estimate_diameter_km`, `_estimate_mass_earth`, `_assign_sig_moon_orbits`, `_place_ring`. `generate_moons()` gains 5 new optional parameters (`planet_diameter_km`, `planet_mass_earth`, `orbit_au`, `star_mass_solar`, `planet_ecc`); when `orbit_au > 0` and `star_mass_solar > 0` the Hill sphere is computed, moons removed/converted if too close, MOR computed, orbit PDs rolled and sorted, ring centre/span placed. `traveller_world_detail.py`: `_moons_for()` gains `orbit_au`, `planet_ecc`, `star_mass_solar` parameters and threads them through; moon attachment loop in `generate_system_detail()` looks up host star and passes orbit data; `attach_detail()` passes `WorldPhysical.diameter_km` and `WorldPhysical.mass` for mainworld (via `mainworld.size_detail`). Moon orbit eccentricity/direction deferred. 22 new tests in new `tests/test_moon_gen.py`; 1036 tests pass. |
| 2026-05-19 | Session 49 — New standalone script `render_system_json.py`: reads a system JSON file produced by `TravellerSystem.to_dict()` and renders a rich self-contained HTML document; no project module imports (stdlib only: `json`, `sys`, `html`, `pathlib`). Renders system header, stars table, habitable zones, world-count chips, orbital survey (with moon sub-rows), mainworld panel (stats grid, trade badges, WorldPhysical/BeltPhysical card, atmosphere detail card, hydrographic detail card), and raw JSON collapsible. Distinguishes `WorldPhysical` vs `BeltPhysical` by checking for `"composition"` key. Pylint 10.00/10. JSON Schema fix: `eccentricity_adjusted` added to `WorldPhysical` branch of `traveller_world_schema.json`; the field was emitted by `WorldPhysical.to_dict()` but absent from the schema, causing `jsonschema.validate()` failures for 1:1 tidally-locked worlds with `orbit_eccentricity > 0.1`. Sample output: `examples/system_seed42.json` and `examples/system_seed42.html` (seed=42, 3-star system). 1008 tests pass. |
| 2026-05-19 | Session 47 — Bug fix #63: `orbital_eccentricity` and `orbital_inclination` flags not wired through TravellerMap endpoints. Three-layer gap: `generate_system_from_map()` did not accept the flags, `_map_system_response()` had no `want_ecc`/`want_incl` params, and neither endpoint handler called the parse helpers. All three layers fixed; flags thread into `generate_orbits()` and stored on `TravellerSystem`. Pylint suppress `too-many-arguments,too-many-locals,too-many-positional-arguments` added to `generate_system_from_map()`. 4 regression tests in `TestGenerateSystemFromMapOrbitalFlags`; 1002 tests pass. |
| 2026-05-18 | Session 43 — Orbital eccentricity implemented (WBH p.27, issue #57). Optional feature gated on `orbital_eccentricity=False` flag. `Star` gains `orbit_eccentricity: float = 0.0`; `OrbitSlot` gains `eccentricity: float = field(default=0.0, init=False)`. `_ECC_TABLE` and `_roll_eccentricity()` added to `traveller_orbit_gen.py`; post-placement pass in `generate_orbits()` computes world/belt/star eccentricities when flag is set. DMs: +2 for stars, +1 per extra star orbited beyond the first, −1 for orbit# < 1.0 if age > 1 Gyr, +1 for belts. `to_dict()` emits `eccentricity`, `orbit_au_min`, `orbit_au_max` when non-zero. `TravellerSystem` stores `orbital_eccentricity` flag. Flag plumbed through `generate_full_system()`, `generate_system_from_world()`, and all API endpoints via new `parse_orbital_eccentricity()` helper. gen-ui and system_map show `e=X.XXX` inline in AU column when eccentricity > 0. No seed disruption when flag is False. 6 new tests in `TestOrbitalEccentricity`; 979 tests pass. |
| 2026-05-20 | Session 56 — Seismic stress implemented (WBH Seismology ~pp.125-128). `traveller_world_physical.py`: new helpers `_compute_rss()` (Residual Seismic Stress: `floor(Size - Age + DMs)²`, DMs for is_moon/moon sizes/density), `_compute_thf()` (Tidal Heating Factor: `PrimaryMass⊕² × (diam/1600)⁵ × e² / (3000 × dist_Mkm⁵ × period_days × WorldMass⊕)`, values < 1 = 0), `_apply_seismic_stress()` (sets all four new fields on WorldPhysical, computes seismic temperature). `WorldPhysical` gains four new `Optional[int]` fields: `residual_seismic_stress`, `tidal_heating_factor`, `total_seismic_stress`, `seismic_temperature_k` (adjusted temperature via `⁴√(T⁴+TSS⁴)`, only when value differs from mean_temperature_k). `apply_moon_tidal_effects()` gains `is_moon: bool = False` parameter; early return for empty moons removed (tidal lock re-run skipped for empty moons but seismic stress always computed). `function_app._apply_mainworld_moon_tidal()` detects `is_moon` via `mw_orbit.world_type == "gas_giant"`, removes guard against no moons. `gen-ui/app.py` call site and `_build_physical_card()` updated to show RSS, THF (when >0), TSS, seismic temperature. `templates/world_card.html` and `render_system_json.py._phys_rows()` updated. `traveller_world_schema.json` gains four new optional fields. Tidal Stress Factor (WBH p.126) deferred as issue #67. 22 new tests; 1107 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-21 | Session 59 — Two fixes from WBH p.125 review. (1) Rename: `tidal_heating_factor` → `tidal_seismic_stress` throughout (field, JSON key, schema, gen-ui label, world_card.html, render_system_json.py, tests). Private function `_compute_thf()` renamed `_compute_tidal_ss()`. Formula unchanged; terminology now matches WBH. (2) Bug fix: `apply_moon_tidal_effects()` crashed with `AttributeError` when called for a Size 0 (belt) mainworld — `BeltPhysical` has no `density`, `axial_tilt`, or seismic fields. Fixed with `isinstance(physical, WorldPhysical)` early-return guard. 1189 tests pass; pylint 10.00/10. |
| 2026-05-21 | Session 58 — Surface tidal amplitude implemented (WBH pp.107–108, issue #68). `traveller_world_physical.py`: new helpers `_moon_mass_earth(size)` (Terran-density estimate: `(size×1600/12742)³`), `_star_tidal_effect_m(star_mass_solar, size, orbit_au)` (`star_mass×size / (32×AU³)`), `_moon_tidal_effect_m(moon, planet_size)` (`moon_mass×size / (3.2×(orbit_km/1e6)³)`, excludes rings and moons with no orbit_km), `_compute_tidal_amplitude(star_mass, size, orbit_au, moons)`. `WorldPhysical` gains `tidal_amplitude_m: Optional[float]`; `generate_world_physical()` sets star-only preliminary value; `apply_moon_tidal_effects()` adds moon contributions and updates in-place. `to_dict()` emits `tidal_amplitude_m` (4 dp) when set. `traveller_world_schema.json` gains `tidal_amplitude_m` in WorldPhysical branch. Display updated in gen-ui `_build_physical_card()`, `templates/world_card.html`, and `render_system_json.py`. Also fixed: pre-existing `_ZONE_OBJECT_NAME → ZONE_CSS_CLASS` name typo in gen-ui; `test_moon_lock_occurs_when_dm_high_enough` patched deterministic (broken-lock check caused intermittent flap). 21 new tests in 5 new test classes; 1189 tests pass; pylint 10.00/10. |
| 2026-05-21 | Session 57 — Enum types (issue #38) and centralised display tables (issue #39). New `world_codes.py`: `StarportCode`, `TemperatureCategory`, `TradeCode`, `TravelZone` (`StrEnum`); `AtmosphereCode` (`IntEnum`). `parse_uwp()` now raises `ValueError` (with chained context) for wrong length, missing separator, unrecognised starport, or non-hex code position. New `World._validate_world_codes()` static method validates starport, atmosphere, temperature, trade codes, travel zone, and all integer range fields in `from_dict()`. New `tables.py`: single canonical source for 7 display-layer lookup tables (`SIZE_DIAMETER_LABEL`, `SIZE_GRAVITY_LABEL`, `POPULATION_RANGE`, `TRADE_CODE_FULL`, `BASE_FULL`, `ZONE_CSS_CLASS`, `TIDAL_STATUS_LABELS`); all five consumers updated. Pyright CI: `.github/workflows/typecheck.yml` added (runs on every push/PR to `main`); `pyrightconfig.json` updated; `requirements-dev.txt` gains `pyright>=1.1.0`; 8 pre-existing Pyright errors fixed (stored-boolean narrowing → `isinstance`, `Moon` forward reference via `TYPE_CHECKING`, `hasattr` → `isinstance`). `.pylintrc` restored after accidental deletion in f9f6ca1; `duplicate-code` and `cyclic-import` suppressions documented. 1168 tests pass; pylint 10.00/10 maintained. |
| 2026-05-22 | Session 60 cont. — Tidal Stress Factor implemented (WBH p.126, issue #67). `WorldPhysical` gains `tidal_stress_factor: Optional[int]` = `floor(tidal_amplitude_m / 10)`. Computed inside `_apply_seismic_stress()` via `_compute_tidal_amplitude()` (already implemented in Session 58); `tidal_amplitude_m` assignment moved here from `apply_moon_tidal_effects()` caller. `total_seismic_stress` now = RSS + Tidal Seismic Stress + TSF. `to_dict()` and JSON schema updated; display added to gen-ui, `world_card.html`, `render_system_json.py` (order: tidal_seismic_stress → TSF → RSS → total). Seismic display order also corrected this session (was RSS → tidal → total). 6 new tests; 1195 tests pass; pylint 10.00/10. |
| 2026-05-22 | Session 60 — gen-ui issue #58: removed "Orbital Eccentricity" and "Orbital Inclination" checkboxes from `gen-ui/app.py`. Both flags are now always `True` when "System detail" is enabled. Updated all three call sites (`_on_generate`, `_do_travellermap_generation`, `_show_disambiguation_dialog`). `context/gen-ui.md` updated. No tests affected. |
| 2026-05-20 | Session 55 — Bug A fix (issue #66): removed superseded `hz_deviation_to_raw_roll()` call from orbital context note in `traveller_system_gen.py`; note now shows orbit#, AU, HZ deviation only. Verified all three p.108 Basic Mean Temperature rules remain correctly implemented. v1.2-new-features.txt created. Context updated for v1.3.0. |
| 2026-05-20 | Session 54 — Basic Mean Temperature implemented (WBH p.47). `traveller_world_physical.py`: new `_MEAN_TEMP_TABLE_K` dict (rolls 0–12, K values authoritative), `_MEAN_TEMP_ATM_DM` inline dict (copy of `TEMPERATURE_DM` to avoid circular import), `_orbit_dm_for_mean_temp(hz_deviation)` (DM+4+1/0.5 inside HZCO-1; DM-4-1/0.5 outside HZCO+1; 0 in HZ), `_compute_mean_temperature(hz_deviation, atmosphere)` (modified roll = 7 + orbit_dm + atm_dm; table lookup; -5K/step below 0; +50K/step above 12; minimum 3K). `WorldPhysical` gains `mean_temperature_k: Optional[int] = field(default=None, init=False)`; `to_dict()` emits `mean_temperature_k` when set. `generate_world_physical()` gains `hz_deviation: Optional[float] = None` parameter; sets `wp.mean_temperature_k` when provided. Both call sites updated: `function_app._attach_mainworld_physical()` and `gen-ui/app.py._finish_system_generation()` now pass `hz_deviation`. All four HTML templates (`world_card.html`, `world_list.html`, `system_card.html`) gain a Mean Temperature row after Day length. `render_system_json.py._phys_rows()` emits the field when present. `traveller_world_schema.json`: `mean_temperature_k` added to WorldPhysical. Pre-existing unused imports removed from `test_world_physical.py`; `apply_moon_tidal_effects()` pylint suppress added; two pre-existing long lines in `gen-ui/app.py` fixed. 17 new tests; 1085 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-20 | Session 53 — HTML template refactor (issue #40). Added `Jinja2>=3.1.0` to `requirements.txt`. New `html_render.py` with module-level Jinja2 environment (`autoescape=True`). New templates: `world_card.html`, `world_list.html`, `system_card.html`. `World.to_html()` refactored from ~350 lines of f-string HTML to a 3-line template call; new `_world_html_ctx(world)` module-level helper pre-computes all display values. `TravellerSystem.to_html()` similarly refactored from ~400 lines to template delegation. Multi-world CLI `--html` output fixed: replaced fragile regex stitching with `world_list.html` template. README updated with install script guidance for all three platforms (macOS/Linux, Windows PowerShell, Windows Command Prompt). `render_system_json.py` refactored to use new `system_detail.html` template (richer than `system_card.html`: adds Period column, Notes column, HZ zones section, orbit-count chips); all `_render_*` HTML-string functions replaced with data-builder functions returning plain dicts. Mainworld notes (`World.notes`) added to `system_card.html` and `mw_data` in `TravellerSystem.to_html()` — previously missing from system HTML output (visible in gen-ui but absent from HTML); `system_detail.html` already had notes from the initial implementation. 1068 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-22 | Session 61 — Biomass Rating implemented (WBH pp.127-131). New `generate_biomass_rating()` in `traveller_world_detail.py` (2D + DMs from atmosphere, hydrographics, age, temperature; DM clamped to [-12,+4]; Special Cases 1 and 2 implemented). `_apply_biomass()` called as last step in `attach_detail()` to preserve RNG seed order for all prior fields. `WorldDetail` gains `biomass_rating: Optional[int]`. `World` gains `biomass_rating: Optional[int]` (set only when Mainworld Detail enabled). gen-ui: orbit Notes column shows 'Biomass N' when > 0; moon sub-row Notes column likewise; mainworld physical card always shows biomass rating. `traveller_world_schema.json` updated. Module-level `too-many-lines` suppression added to `traveller_world_detail.py` (now 1155 lines). 65 new tests; 1260 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-24 | Session 63 — Biocomplexity Rating implemented (WBH pp.127-131). New `generate_biocomplexity_rating()` in `traveller_world_detail.py` (2D−7 + min(biomass,9) + DMs; atmosphere not 4–9 DM−2; low-oxygen taint DM−2; age DMs using worst-at-boundary rule; min result 1). Helper `_set_biocomplexity()` extracted to module level. `_apply_biomass()` now also computes biocomplexity for all bodies with biomass > 0. `WorldDetail` gains `biocomplexity_rating: Optional[int]`. `World` gains `biocomplexity_rating: Optional[int]`. `BIOCOMPLEXITY_DESC` dict added to `tables.py`. System card orbit table gains "Biosphere" column showing "B, C" eHex for worlds with life. System card mainworld section gains "Native life" inner-card showing biomass + biocomplexity. World card Physical characteristics section shows biomass + biocomplexity when biomass > 0. Rare Earth Variant deferred to `context/deferred-features.md`. 1276 tests pass (no new tests needed); pylint 10.00/10 on all modified files including tables.py (pre-existing missing-module-docstring fixed). |
| 2026-05-24 | Session 64 — gen-ui radio toggle: replaced "System detail" + "Mainworld detail" checkboxes with a `QRadioButton` pair ("Mainworld only" / "Full detail") backed by a `QButtonGroup`. Full detail enables NHZ, oxygen, and map button; mainworld only disables and clears them. `_on_system_detail_toggled` renamed to `_on_detail_toggled`. |
| 2026-05-24 | Session 64 cont. — gen-ui two-tab display: `_show_system_summary()` now builds a `QTabWidget` (Tab 1 "System" = stellar + orbits cards in a `QScrollArea`; Tab 2 "Mainworld" = `QWebEngineView` showing `mw.to_html()`; default tab is Mainworld). `_build_system_summary_header()` return type changed from tuple to `QWidget` (orbit toggle checkbox removed; now superseded by tab switching). |
| 2026-05-24 | Session 64 cont. — Biocomplexity DM test coverage: added 41 new tests to `tests/test_biomass.py` covering every DM condition (`TestBiocomplexityAtmosphereDM`, `TestBiocomplexityLowOxygenDM`, `TestBiocomplexityAgeDM`, `TestBiocomplexitySpecialCases`). Schema gap fixed: `biocomplexity_rating` was missing from `traveller_world_schema.json` since Session 63. |
| 2026-05-24 | Session 64 cont. — Biomass temperature DM compliance fix: "High temperature" rows (DM-2 above 353K, DM-4 below 273K) were missing from `generate_biomass_rating()`. The WBH p.127 table has 5 temperature rows; the code only applied 3 (Mean temperature rows). Fix: when `mean_temp_k > 353` apply DM-2+DM-4 = DM-6; when `mean_temp_k < 273` apply DM-4+DM-2 = DM-6. Combined DMs now match the footnote simplified values (frozen/boiling → -6). 7 existing K-path tests updated (expected values changed); 3 new tests added (boundary at 353K/273K, footnote equivalence). 1320 tests pass; pylint 10.00/10. |
| 2026-05-24 | Session 64 cont. — Native sophont generation (WBH p.131). New `generate_sophont_checks(biocomplexity, age_gyr) -> tuple[bool, bool]` in `traveller_world_detail.py`. Current sophont: 2D + min(bio,9) − 7 ≥ 13; extinct: same + DM+1 if age > 5 Gyr (only when current fails). Triggered inside `_apply_biomass()` when biocomplexity ≥ 8. `World` gains `native_sophont: bool` and `extinct_sophont: bool` fields; both False by default; emitted to JSON only when True. World card biological detail section shows "Extant" or "Extinct (evidence present)". 11 new tests in `TestSophontChecks`. JSON schema gains `native_sophont`, `extinct_sophont`, and (fix) `biocomplexity_rating` properties. 1317 tests pass; pylint 10.00/10. |
| 2026-05-24 | Session 65 — Advanced mean temperature implemented (WBH pp.47-50). New `_roll_albedo()` (rocky/icy/icy-far base by density/hz_deviation; atmosphere thin/mid/very-dense/heavy modifiers; hydrographics modifier; clamped [0.02, 0.98]) and `_roll_greenhouse_factor()` (initial 0.5×√bar; standard/exotic/extreme atmosphere modifier) in `traveller_world_physical.py`. New public `generate_advanced_mean_temperature(physical, atmosphere, hydrographics, pressure_bar, luminosity, orbit_au, hz_deviation, orbit_eccentricity=0.0, star_mass=1.0)` sets five new `WorldPhysical` fields: `albedo`, `greenhouse_factor`, `advanced_mean_temperature_k`, `high_temperature_k`, `low_temperature_k`. Mean formula: T = 279 × ⁴√(L × (1-A) × (1+G) / AU²). High/Low use variance factors (Steps 1-9): `_axial_tilt_factor()` (sin of normalized tilt, orbital-year modifiers), `_rotation_factor()` (√|day|/50, capped 1.0, 1:1-lock → 1.0), `_geographic_factor()` ((10-HYD)/20), combined as luminosity modifier through atmospheric factor (1+bar), applied to high/low luminosity × near/far AU. BeltPhysical guard added. gen-ui: "Advanced temperature" checkbox (Full detail only); call site moved BEFORE `_attach_detail()` so advanced temps are on the physical object when `_apply_biomass()` runs. `generate_biomass_rating()` gains `high_temp_k` parameter: "High temperature" DM rows use `high_temp_k` (falls back to `mean_temp_k` proxy); "Mean temperature" rows use `mean_temp_k`; `_apply_biomass()` reads `advanced_mean_temperature_k` and `high_temperature_k` off the physical object via `getattr`. `templates/world_card.html` gains 5 new display rows. `traveller_world_schema.json` gains 5 new optional fields. 72 new tests in 6 new test classes; 1392 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-22 | Session 62 — Moon orbital eccentricity and inclination implemented (WBH p.76). `_roll_eccentricity` → `roll_eccentricity` and `_roll_inclination` → `roll_inclination` in `traveller_orbit_gen.py` (made public). `traveller_moon_gen.py` imports both; `Moon.orbit_retrograde: bool` replaced by `Moon.orbit_inclination: float` (retrograde implied by inclination > 90°). `generate_moons()` rolls both for each significant moon after orbit placement. Emitted to JSON as `orbit_eccentricity` (4 d.p.) and `orbit_inclination` (2 d.p.) when > 0. gen-ui Ecc/Incl column in moon sub-rows now shows `ecc/incl°`. Pyright fix: `age_gyr: float = system.stellar_system.primary.age_gyr or 0.0` in `_apply_biomass()` (was `float | None`). Test imports updated for renamed functions; 9 new tests in `TestMoonEccentricityInclination`; 1276 tests pass; pylint 10.00/10. |

| 2026-05-25 | Session 67 — Hydrographic composition Phase 2 implemented (WBH pp.91-92). `HydrographicDetail` gains `fluid_type: Optional[str]` field determined by temperature zone (Boiling→Sulfuric Acid, Hot/Temperate→Water, Cold→Ammonia, Frozen→Liquid Hydrocarbons). New `_FLUID_TYPE_BY_TEMP` lookup dict, `_NO_SURFACE_LIQUID_ATMS` frozenset, and `_fluid_type(atmosphere, temperature)` pure helper. `generate_hydrographic_detail()` gains keyword-only `atmosphere: int = 0` and `temperature: str = "Temperate"` parameters; `fluid_type` is None for desert worlds (hydro 0) and gas-atmosphere worlds (codes 16/17). `to_dict()` emits `fluid_type` when set. 8 call sites updated across `traveller_system_gen.py`, `function_app.py` (×4), `traveller_map_fetch.py`, `gen-ui/app.py` (×2). Display: "Fluid type" row added after "Surface liquid" in `world_card.html`, `system_card.html`, `world_list.html`, `system_detail.html`; `render_system_json.py._mw_ctx()` gains `hyd_fluid_type`; `World.summary()` gains fluid type line. JSON schema updated with `fluid_type` optional string property. `context/deferred-features.md` "World physical detail" row updated to Complete. 20 new tests in `TestFluidTypeHelper` and `TestFluidTypeIntegration`; 1425 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-25 | Session 66 — Stellar day vs sidereal day implemented (WBH p.106, issue #20). New `_compute_stellar_day(sidereal_h, orbital_period_h, tidal_status)` pure helper in `traveller_world_physical.py`: prograde formula `(T_sid×T_orb)/(T_orb−T_sid)`; retrograde formula `(T_sid×T_orb)/(T_orb+T_sid)`; returns None for 1:1_lock and prograde edge cases where T_sid≥T_orb. `WorldPhysical` gains `stellar_day_hours: Optional[float]` field; set in `generate_world_physical()` when orbital data is present and recomputed in `apply_moon_tidal_effects()` (reuses existing `period_h` variable). `to_dict()` emits `stellar_day_hours` when not None. Display: "Stellar day" row added after "Day length" in `world_card.html`, `system_card.html`, `world_list.html`, `render_system_json.py._phys_rows()`, and `World.summary()` text output. 13 new tests (8 direct unit tests of `_compute_stellar_day` covering every branch + edge case; 5 integration tests through `generate_world_physical()`); 1405 tests pass; no new RNG calls. |
