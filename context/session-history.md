# session-history.md — Session history

Append one row per session. Never delete entries — they explain why the code is
shaped the way it is.

| Date | Summary |
| 2026-06-17 | Session 129 — Cultural detail traits 3–8 + DXUS-CPEM profile. Added Uniqueness (2D + Starport/Diversity/Xenophilia DMs; labels Indistinct/Typical/Distinct/Exotic), Symbology (2D + Gov D/E, TL, Uniqueness DMs; labels Mundane/Moderate/Prominent/Pervasive), Cohesion (2D + Gov, Law, PCR, Diversity DMs; labels Individualistic/Moderate/Communal/Collectivist), Progressiveness (2D + Pop, Gov, Law, Diversity, Xenophilia, Cohesion DMs; labels Moribund/Conservative/Moderate/Progressive/Innovative), Expansionism (2D + Gov A/C+, Diversity, Xenophilia DMs; labels Insular/Moderate/Expansive/Imperialist), Militancy (2D + Gov A+, Law, Xenophilia, Expansionism DMs; labels Peaceful/Moderate/Aggressive/Militaristic). Full DXUS-CPEM profile string (9 chars: 4 eHex + hyphen + 4 eHex). `generate_culture_detail` gained `tech_level` parameter for Symbology DMs. `CultureDetail` now has 16 trait fields + `cultural_profile`. `to_dict()` and `from_dict()` cover all fields with backward-compat defaults of 1. World card HTML updated with all 8 trait rows. 2460 tests pass; pylint 10.00/10. |
| 2026-06-17 | Session 128 — Xenophilia cultural trait. Added `_xenophilia_dm()` (DMs from Population, Government D/E, Law A+, Starport A/B/D/E/X, Diversity feedback), `_xenophilia_label()` (Xenophobic ≤3, Moderate 4–8, Welcoming 9+). `xenophilia` and `xenophilia_label` added to `CultureDetail`; profile extended to 2 eHex chars. Tests: `TestXenophiliaBounds`, `TestXenophiliaDMEffects`, `TestXenophiliaLabels`. |
| 2026-06-17 | Session 127 — Cultural Diversity trait (issue #99). New module `traveller_world_culture_detail.py`. `CultureDetail` dataclass, `generate_culture_detail()` (2D + Pop/Gov/Law/PCR DMs), `attach_culture_detail()`. Labels: Monolithic ≤3, Homogeneous 4–5, Diverse 6–8, Multicultural 9–11, Balkanised 12+. Wired into `system_pipeline.py` `run_detail_pipeline()` under `want_social_detail` flag. Social detail checkbox added to gen-ui Options dialog and `?social_detail=true` parameter on all FastAPI endpoints. World card template gains Culture section. Tests: `TestNoneGuard`, `TestDiversityBounds`, `TestDMEffects`, `TestLabels`. |
| 2026-06-17 | Session 130 — T5 Cultural Extension (Cx) conversion (issue #150). When reading a mainworld from TravellerMap, the Cx field (4 eHex: HASS) is available and converted to the first four cultural traits per WBH rules: H→Diversity [clamped], A→Xenophilia [clamped with Importance], S→Uniqueness [ceil(S×3/2)], S2→Symbology [clamped]. Remaining four traits (CPEM) are rolled with dice+DMs using derived Diversity/Xenophilia. (1) `MapWorldData` adds `cx: str = ""` and `importance: int = 0` fields; `fetch_world_data()` extracts from TravellerMap `"Cx"` and `"Ix"` fields. (2) `generate_system_from_map()` stamps `world.cx` and `world.importance` after `reconstruct_world()` if Cx is non-empty. (3) `generate_culture_detail_from_cx()` function added to `traveller_world_culture_detail.py` with clamping logic and rolled traits. `_parse_cx_string()` and `_from_ehex()` helpers added. (4) `attach_culture_detail()` routes to from_cx when `world.cx` is present; otherwise uses standard generation. (5) gen-ui/app.py updated to check for `world.cx` before calling culture detail generation. (6) Azure API copies synced. (7) New tests: `TestParseCxString`, `TestFromCxNoneGuard`, `TestFromCxDiversityMapping`, `TestFromCxXenophiliaMapping`, `TestFromCxUniquenessMapping`, `TestFromCxSymbologyMapping`, `TestFromCxRolledTraits`, `TestAttachCultureDetailCxPath`. 30 new tests; 2460 tests pass; pylint 10.00/10. |
| 2026-06-16 | Session 126 — Azure monitoring and cost controls. (1) Azure Monitor OpenTelemetry: `configure_azure_monitor()` wired into `fastapi/app.py` when `APPLICATIONINSIGHTS_CONNECTION_STRING` env var is present; adds per-request traces, dependency tracking, and log forwarding to Application Insights. Import guarded with try/except so local dev is unaffected. `azure-monitor-opentelemetry>=1.6.0` added to `azure-api/requirements.txt`. (2) Daily execution quota: `scripts/create_azure_function_app.sh` now sets `dailyMemoryTimeQuota=1024000000` (1,000 GB-seconds/day, roughly 25,000 requests) to protect against runaway cost; well within the free tier (400,000 GB-s/month). Quota stops all invocations when hit, restarting at midnight UTC. (3) New script `scripts/set_azure_budget.sh`: creates or updates a monthly Cost Management budget ($10/month default, user-configurable) with email alerts at 80% and 100% thresholds. Uses `az rest` to PUT directly to the budgets API endpoint. Run post-provisioning via `az login && bash scripts/set_azure_budget.sh`. (4) `/update-docs` skill pinned to `claude-haiku-4-5-20251001` model via frontmatter. 2124 tests pass; pylint 10.00/10. |
| 2026-06-16 | Session 125 — Bug fix: vacuum world high/low temperature range (issue #144). `generate_advanced_mean_temperature()` in `traveller_world_atmosphere_detail.py` used a 10-bar pressure fallback whenever `pressure_bar is None`. Atmosphere code 0 has no entry in `ATMOSPHERE_PRESSURE_SPAN_BAR` so its `pressure_bar` was always `None`, triggering the fallback and dividing the luminosity variance modifier by 11 instead of 1. Added explicit `atmosphere == 0 → eff_pressure = 0.0` guard before the `None` check. Result: vacuum worlds now show physically correct diurnal temperature extremes (3 K night / 420 K day for 720-hour day at 0.58 AU) instead of ≈±6 K. 2124 tests pass; pylint 10.00/10. |
| 2026-06-15 | Session 124 — pytest-qt GUI test suite for gen-ui/app.py. New `tests/test_genui_app.py` with 121 tests across 13 classes: `TestAppWindowStartup`, `TestSourceRadioToggle`, `TestOptionsDialogConstruction`, `TestOptionsDialogProperties`, `TestOptionsIntegration`, `TestSeedHandling`, `TestTravellerMapErrors`, `TestProceduralWorldGeneration`, `TestProceduralSystemGeneration`, `TestDarkMode`, `TestSystemMapWindow`, `TestSurveyFormWindow`, `TestHeaderButtons`. Two test stubs: `_FakeSettings` (QSettings that never touches disk) and `_MockWebView` (QWebEngineView that captures HTML without Chromium). Module loaded via `importlib.util.spec_from_file_location` as `genui_app` to avoid name clash with `fastapi/app.py`. `pytest.ini` updated with `qt_api = pyside6`. Fixtures: `fake_settings`, `no_webengine`, `app_win`, `sample_system` (session-scoped), `system_app_win`. Three key design notes: (1) `isVisibleTo(parent)` used instead of `isVisible()` for un-shown dialog child widgets; (2) eccentricity/inclination sub-options are cleared by `_on_group_toggled(False)` and must be tested with `full_system=True`; (3) seed reproducibility tests use `blockSignals`+`_seed_auto=False` to force the seed field without triggering the auto-seed guard. docs/uat-plan.md updated from Session 32 to Session 124: obsolete tests removed (Stellar & Orbits toggle, Open in Browser, TXT save format), new sections for Options dialog, tabs, Dark Mode, Survey Form, File menu; 82 test cases total (was 58). 2250 tests pass. |
| 2026-06-15 | Session 123 — Belt profile strings + IISS Class 0/I Survey form. (1) `BeltPhysical.profile_str` computed property added to `traveller_belt_physical.py` — returns WBH Class III shorthand `S-CC.CC.CC.CC-B-R-#-s` (span AU, four-component composition percentages dot-separated, bulk, resource rating, Size 1 count, Size S count). `to_html()` in `traveller_system_gen.py` appends `Profile: <profile_str>` to `note_parts` for belt orbits. `system_body_table()` in `traveller_world_detail.py` appends `Profile: <profile_str>` to `notes_suffix` for the CLI display. 7 new tests in `TestBeltPhysicalProfileStr`. (2) `TravellerSystem.to_survey_form_html()` method added to `traveller_system_gen.py`; renders the new `templates/survey_class0i.html` Jinja2 template with designation, age, stellar count, star rows (component, class, mass, temp, diameter, luminosity, orbit#, AU, ecc, period in years, HZCO), and notes (sub-year orbital periods converted to standard days with footnote superscripts). Period column always `f"{yr:.3f}y"`; sub-year note only. CSS variables for full light/dark theming (`[data-theme=dark]`, `[data-theme=light]`, `@media`); labels sans-serif, data fields Courier New italic; Notes/Comments cells `height:180px; vertical-align:top; white-space:pre-line`. (3) `gen-ui/app.py`: `SurveyFormWindow(QMainWindow)` added (before background worker); new instance attrs `_survey_windows`, `_survey_btn`, `_survey_combo`; Survey Form button + QComboBox added to `_build_system_summary_header()`; `_on_survey_clicked()` applies `_themed_html()` then opens window. (4) `fastapi/app.py`: both `include_mw_card` JSON response sites now include `"survey_class0i_html": system.to_survey_form_html()`. (5) `fastapi/static/system.html`: Survey tab panel + `tab-btn-survey` button added; seed bar survey button + select added; `showTabs()` accepts `surveyHtmls`; `_applyTheme()` reloads survey iframe; four `showTabs()` call sites updated. 2129 tests pass; pylint 10.00/10. |
| 2026-06-12 | Session 122 — FastAPI UI polish: (1) System map light/dark theming — `fastapi/app.py` defines `_PALETTE_LIGHT` (bg=#f4f0e4) via `dataclasses.replace`; `system.html` `buildMapUrl()` passes `white_bg=true` when theme=light; `_applyTheme()` re-fetches the map on toggle; map container CSS uses `var(--bg)` instead of hardcoded #0d1117. (2) Removed redundant Mongoose disclaimer `<p>` from bottom of all four card templates (`system_card.html`, `system_detail.html`, `world_card.html`, `world_list.html`) — disclaimer already present in parent page footer. (3) Moon orbital table column alignment: moon rows now have empty `#` cell so pd_str lands under Orbit#, km_str under AU, ecc_incl/period_str in their correct columns; applied to both `system_card.html` and `system_detail.html`. (4) Restored `run-gui.command` from backup; removed `run-gui.command`/`run-gui.bat` from `.gitignore`. 2122 tests pass. |
| 2026-06-12 | Session 121 — WBH §5 sub-tech levels: all QoL/Transportation + Military categories implemented in `traveller_world_tech_detail.py`. `_sub_tl()` inner helper gained `dm` and `base` parameters. `generate_tech_detail()` gained `size` and `trade_codes` parameters. Energy (base=High TL, Pop9+/Industrial DMs), Electronics (base=Energy TL, Pop1-5/Pop9+/Industrial DMs), Manufacturing (base=Electronics TL, Pop1-6/Pop8+/Industrial DMs), Medical (base=Electronics TL, starport floors corrected A:6/B:4/C:2, Rich/Poor DMs), Environmental (base=Manufacturing TL, habitability DM), Land (base=Energy TL, Hydro10/PCR DMs), Sea (base=Energy TL, hydro=0 no longer forces 0 — DM -2), Air (base=Energy TL, atm DMs at TL 0-7 only), Space (base=Manufacturing TL, bounds=[min(energy,mfg)-3, min(energy,mfg)], Size/Pop/Starport DMs; isolated regions deferred), Personal Military (base=Manufacturing TL, upper=Electronics TL, lower=0 or min(Mfg,Elec) when Law=0, Gov0/7+2, Law0/D++2, Law1-4/9-C+1; old code wrongly forced 0 when Law=0). Heavy Military (base=Manufacturing TL, upper=Manufacturing TL, lower=0, Pop1-6/-1, Pop8++1, Gov7/A/B/F+2, LawD++2, Industrial+1; old code had no DMs). Government 7 DM+2 applied at world level for both military sub-TLs; per-nation law/pop DMs deferred. `_tech_detail_for_det()` and `attach_tech_detail()` updated to pass `size` and `trade_codes`. New test classes for all sub-TL categories; `TestBoundsInvariants` expanded. 2122 tests pass; pylint 10.00/10. |
| 2026-06-12 | Session 120 — Pipeline unification (`system_pipeline.py`) + Session 119 code completion + docs. (1) New `system_pipeline.py`: `PipelineOptions` dataclass + `run_detail_pipeline()` consolidates post-`generate_full_system()` orchestration shared by CLI, gen-ui, and FastAPI — replaces ~130 lines of duplicated inline code in each entry point. Pipeline order: `_attach_physical` → `attach_detail` → `attach_body_names` → `_apply_moon_tidal` → [optional `select_mainworld`] → `apply_mainworld_social` → [if swapped: `reattach_mainworld_orbit` + `_apply_moon_tidal`] → `apply_secondary_social` → [optional `attach_population_detail` / `attach_government_detail` / `attach_law_detail` / `attach_tech_detail`]. `BeltPhysical` guard added to `_apply_moon_tidal()` in both `system_pipeline.py` and `fastapi/app.py`. `context/generation-pipeline.md` created; CLAUDE.md routing table updated for `system_pipeline.py` and test count updated to 2060. (2) Session 119 remaining code committed: `_minimal_tl()` gas/helium fix, `_sah_digit()` EHEX helper, FastAPI ecc/incl checkboxes, moon column alignment. (3) TLM table for High/Low common TL confirmed already implemented (`_TLM_TABLE`, `_tlm()` in `traveller_world_tech_detail.py`) — no code changes required. 16 new tests in `tests/test_system_pipeline.py`; 2060 tests pass; pylint 10.00/10 on all changed files. |
| 2026-06-11 | Session 119 — Generation pipeline alignment (CLI, gen-ui, FastAPI). (1) FastAPI `_attach_mainworld_physical()` now accepts and passes `rng` to `generate_world_physical()` — physical dice rolls now advance the shared seeded RNG so UWP results match gen-ui for the same seed. (2) CLI `main()` was missing `apply_mainworld_social()` entirely — the mainworld now receives starport, population, government, law level, and TL. CLI pipeline now mirrors FastAPI: `generate_world_physical()` → `attach_detail()` → `attach_body_names()` → `apply_mainworld_social()` → `apply_secondary_social()`. (3) gen-ui had hardcoded `orbital_eccentricity=True, orbital_inclination=True`; two new checkboxes added to `_OptionsDialog` and persisted in QSettings. (4) FastAPI `system.html` gains Eccentricity and Inclination checkboxes, wired into all URL builders and `_lastGen` stores. (5) `_minimal_tl()` in `traveller_world_detail.py` now returns 99 for atmosphere ≥ 16 (Gas/Helium NHZ codes G/H) — these worlds are never colonisable. (6) `_sah_digit()` helper in `traveller_world_tech_detail.py` replaces `int(..., 16)` to handle EHEX atmosphere codes G/H (values 16/17) without `ValueError`. (7) Moon row column alignment fixed in `system_card.html` and `system_detail.html`. 2044 tests pass; pylint 10.00/10 on all changed files. |
| 2026-06-11 | Session 118 — Mongoose Traveller theme + copyright footer applied to all FastAPI web pages; iframe blank-display bug fixed. (1) App shells (`index.html`, `system.html`): amber-on-dark colour scheme (`#c87828` accent, `#0c0e14` bg, `#ddd4b0` text), uppercase header with amber 2 px bottom border, transparent iframe background, Mongoose Publishing copyright footer. (2) Card templates (`world_card.html`, `world_list.html`, `system_card.html`, `system_detail.html`): warm parchment light mode (`#f9f5ee`/`#eeeadb`/`#e4dfc8`), warm-dark mode (`#141210`/`#1d1b17`/`#222018`), section/inner labels amber-orange (`--color-accent`/`--acc`), copyright footer on every rendered document. (3) `loadFrame()` and `showFrame()` deferred-visible fix: `frame.classList.add("visible")` moved into the `onload` `requestAnimationFrame` callback so the iframe is only shown after its `srcdoc` has fully loaded — eliminates intermittent blank mainworld card on first generate. 2044 tests pass. |
| 2026-06-11 | Session 117 — FastAPI bug fixes: issue #138 (mainworld orbit slot SAH wrong + duplicate social detail), CSP blob: URL, and iframe height race. (1) Issue #138: `_attach_mainworld_physical()` was called after `attach_detail()` in 7 endpoints; moved before `attach_detail()` in all of them so physical mutations (runaway greenhouse) are applied before the orbit slot SAH is copied. The `is_mainworld_candidate` guard in `attach_population_detail()`, `attach_government_detail()`, and `attach_tech_detail()` was already in place. (2) CSP `img-src 'self'` → `img-src 'self' blob:` so the `loadMap()` blob URL pattern is not blocked by `_SecurityHeadersMiddleware`. (3) `loadFrame()` `onload` handler wrapped in `requestAnimationFrame` + `h > 0` guard to fix intermittent mainworld card blank display caused by `scrollHeight = 0` before layout completion. 2044 tests pass. |
| 2026-06-10 | Session 116 — FastAPI mainworld UWP mismatch fix + gen-ui RNG threading + Select mainworld option. FastAPI: added `include_mw_card` query param to `/api/system/full` and `/api/map/system/full`; when `format=html&include_mw_card=true` both endpoints return `JSONResponse({"sys_html":…,"mw_html":…})` so the mainworld card comes from the same generation as the system table (no second API call). `parse_include_mw_card()` helper added to `fastapi/helpers.py`. Frontend (`system.html`) updated to use single fetch for both full modes. gen-ui: threaded explicit `random.Random(seed)` through all pipeline calls (`generate_full_system`, `generate_world_physical`, `_attach_detail`, `_select_mainworld`, `apply_mainworld_social`, `_reattach_mainworld_orbit`, `apply_secondary_social`) so secondary world details and mainworld selection use the same RNG continuation as FastAPI. Added "Select mainworld" checkbox to `_OptionsDialog` sub-widget (QSettings key `opt_select_mw`); select_mainworld is now opt-in instead of always-on. 2044 tests pass; pylint 10.00/10. |
| 2026-06-10 | Session 115 — TravellerMap SVG endpoint + FastAPI security hardening. New: `GET /api/map/system/svg` — fetches canonical UWP from TravellerMap and returns `image/svg+xml` via `build_svg()`; orbit slot `world_type` values set by `_reconcile_orbit_types()` so the map honours canonical PBG world/belt/GG counts (9 new tests). `system_map.py` CLI extended with `--sector`/`--hex` flags for TravellerMap mode. Security: `_SecurityHeadersMiddleware(BaseHTTPMiddleware)` added to `fastapi/app.py` emitting `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and a full CSP on every response. `setStatus()` XSS fixed in both HTML files (spinner kept in `innerHTML`; user text via `textContent`). Iframes given `sandbox="allow-scripts"`. `loadMap()` replaced with `fetch()`→content-type check→blob URL pattern. localStorage enum fields validated against allowlists before restore. 2015 tests pass; pylint 10.00/10. |
| 2026-06-09 | Session 114 — FastAPI bug fix + new `/api/map/system/full` endpoint. Bug: `/api/system/svg` was registered after `/api/system/{name}`, causing FastAPI to treat "svg" as a name parameter and return JSON instead of SVG. Fix: moved SVG endpoint registration before the `{name}` wildcard routes. New endpoint: `GET/POST /api/map/system/full` — fetches canonical UWP from TravellerMap then unconditionally runs `attach_detail()`, `apply_secondary_social()`, and (optionally) `attach_population_detail()` / `attach_government_detail()` / `attach_law_detail()`. Accepts all optional flags plus `social_detail` and `format`. Registered before `/api/map/system/{name}`. 11 new tests in `TestMapSystemFull`; 141 FastAPI tests pass. |
| 2026-06-07 | Session 113 — Drop-line prominence + inclination-gated orbit depth cues (system_map.py). (1) Drop-line style increased: stroke-width 0.8→1.4, stroke-dasharray "2,3"→"3,3", opacity 0.55→0.75. (2) Near/far orbit transparency split now gated on `abs(ir) >= math.radians(3.0)`: orbits with inclination < 3° draw as a single full-opacity arc (no split) in perspective mode. Applies to both world arcs and companion star dashed arcs. 1946 tests pass; pylint 10.00/10. |
| 2026-06-06 | Session 112 — Perspective orbit depth cues + drop lines (system_map.py). (1) Orbit transparency: each perspective arc is split at n_seg//2 (α=0°, rightmost point) into near half (normal opacity) and far half (40% opacity, min 0.08). Applied to regular world arcs and companion star dashed arcs; non-perspective half-arcs unchanged. (2) Drop lines: in perspective mode a dotted `<line>` is drawn from the bottom edge of each inclined world or companion star sphere to its z=0 projected position on the orbital plane (`smy_z0 = cy − y4·persp_y`). The z=0 y-coordinate is hoisted from the existing shadow-ellipse computation (`_smy_z0` for worlds, `smy_c` for companion stars). Drop line suppressed when displacement < symbol_radius + 1 px. Style: `stroke=palette.axis stroke-width=0.8 stroke-dasharray="2,3" opacity=0.55`. 1946 tests pass; pylint 10.00/10. |
| 2026-06-06 | Session 111 — 3-D sphere gradients, ring systems, and belt-width arcs in system_map.py. (1) `_sphere_gradient_def(color)` / `_sph(color)` helpers generate SVG radialGradient defs (highlight→base→dark, cx=35% cy=30%) and return `url(#sph_…)` fill refs. All filled circles for stars (primary + companions) and worlds (gas giants, terrestrials) now use sphere gradients instead of flat fills. (2) `_gg_ring_px(detail, sphere_r)` detects `Moon.is_ring` moons and returns (inner_px, outer_px) ring radii from `ring_centre_pd`/`ring_span_pd`. (3) `_ring_halves(mx, my, rx_in, rx_out, persp_y)` returns (rear_path, front_path) SVG annulus paths drawn before/after the sphere so the planet sits embedded in the ring plane; rear at opacity 0.40, front at opacity 0.65. (4) Belt marker `<rect>` removed. Belt rendered as filled annular band via `_belt_band_path` using `_orbit_screen_pts` for both inner (r_in = log1p(inner_au)*log_scale) and outer edges (e=0, circular boundaries) — correct perspective projection. Belt band drawn in arc loop then `continue`; clip_attr computed before branch. Fallback (no BeltPhysical): thin stroke arc. Shadow arcs excluded for belt orbits. Sphere gradient defs emitted in `<defs>` from collected star and world colours. 1946 tests pass; pylint 10.00/10. |
| 2026-06-06 | Session 110 — gen-ui light theme final fix (gen-ui/app.py). Root cause identified: Qt's `QSvgRenderer` on macOS produces incorrect/transparent output for SVGs that contain `<filter><feGaussianBlur>` elements (unsupported SVG feature added in v1.5.0 for perspective shadow arcs). The feGaussianBlur filter caused the renderer to silently corrupt its output even though `isValid()` returned True and palette colours were confirmed correct via debug prints. Fix: replaced `QScrollArea + QLabel + QSvgRenderer/QPixmap` rendering pipeline with a single `QWebEngineView` (Chromium, already imported for world-detail HTML). `_render()` now wraps the SVG in a minimal HTML page (`setHtml(html)`) — Chromium renders all SVG features including filters and shows correct palette background. `QSvgRenderer` import block, `QPixmap`, `QPainter`, `QColor`, `QRectF`, `QScrollArea`, `tempfile`, `QUrl`, `QDesktopServices` imports all removed. `_map_lbl`/`_scroll` instance attributes replaced by `_map_view`. 1946 tests pass; pylint 10.00/10. |
| 2026-06-06 | Session 109 — Perspective map companion star + shadow polish (system_map.py). (1) Companion star arc orbit shadows added: blurred shadow arc drawn for inclined companion orbits, matching world orbit shadow logic. (2) Companion star markers changed from ★ text glyph to filled circle using `_star_r_px(st.diameter)` — same rendering path as the primary star. Companion circles get a blurred shadow ellipse on the orbital plane (shadow=True projection). (3) World and star shadows changed from `<circle>` to `<ellipse rx ry=rx·persp_y>` — flat oval matches foreshortening of the orbital plane at the 15° viewing angle. (4) Legend moved to top of first arc zone (`y_top + 14`, compact 13px row pitch) and brightened (opacity 0.75 → 0.92). gen-ui/app.py: `QSvgWidget` and `QScrollArea` now get explicit `background-color` from `self._palette.bg` to override the global dark stylesheet. iso_grid opacity for dark mode raised 0.22 → 0.45. 1946 tests pass; pylint 10.00/10. |
| 2026-06-06 | Session 108 — Perspective system map visual overhaul (system_map.py). (1) Replaced `_orbit_arc_split` near/far dashed rendering with continuous orbit arcs. (2) Shadow arcs for inclined orbits projected onto z=0 plane; drawn with blur filter (`feGaussianBlur stdDeviation=3`). (3) Full ellipse orbits in perspective mode: `half_deg=180°` gives closed 360° polylines clipped per zone via `<clipPath>`. (4) 30° z-rotation (`_ROT_Z=radians(30°)`) applied to all orbit/shadow polylines via `_orbit_screen_pts` helper. (5) Inclination projection corrected to `ry = b·|persp_y·cos(i)+cos_φ·sin(i)|` (rise-above-plane formula). (6) `_orbit_half_deg` and `_orbit_marker` updated with rot_z parameter. (7) Angle symbols (6px arc) drawn at α=0 and α=π crossings of orbit and shadow ellipses. (8) World icon shadows: blurred filled circle at shadow-projected marker position. (9) World icon radii halved. (10) `--perspective` flag now passes `orbital_inclination=True` to `generate_full_system`. New helpers: `_orbit_screen_pts`, `_shadow_orbit_arc`. Module-level `too-many-lines` suppression added. 1946 tests pass; pylint 10.00/10. |
| 2026-06-05 | Session 107 — Orbital plane disc + dashing cleanup (system_map.py). In perspective mode each arc zone now draws a faint `<ellipse>` (rx=max_r, ry=max_r×persp_y) centred on the star, clipped to the zone, with fill-opacity 0.10/0.12 and stroke-opacity 0.30/0.38 (dark/light), below the isometric grid. Belt and empty orbit arcs changed from dashed to solid (dash="none") so dashed lines now exclusively mean "hidden behind the plane" (far-arc from Session 106 near/far split). 1946 tests pass; pylint 10.00/10. |
| 2026-06-05 | Session 106 — Near/far arc split for inclined orbits (system_map.py). New `_orbit_arc_split` helper divides an inclined orbit arc at the rightmost point `(star_cx + a_px, cy)` — the projection of the orbital-plane crossing. In perspective mode, orbits with `inclination > 0` now draw a solid near arc (above the reference plane) and a dashed far arc (stroke-dasharray 5,4, ~45% opacity). Applied to all non-empty, non-companion orbit arcs. 1946 tests pass; pylint 10.00/10. |
| 2026-06-05 | Session 105 — Perspective isometric floor grid (system_map.py). New `_iso_grid` helper draws two families of parallel diagonal lines (slopes ±persp_y) clipped to each arc zone, producing a diamond-tiled floor illusion. Drawn before orbit arcs so the grid sits behind all content. Opacity 0.11 dark / 0.17 light using `palette.axis`; perspective-only (no-op in top-down mode). 1946 tests pass; pylint 10.00/10. |
| 2026-06-05 | Session 104 — Perspective arc centring fix (system_map.py). Two bugs in `_orbit_arc`, `_orbit_marker`, and `_orbit_half_deg`: (1) retrograde inclination (incl > 90°) produced negative `ry` via `cos(incl) < 0`, causing arcs to be drawn with centres far to the right of the star; (2) eccentric orbits used `ex = star_cx + a_px * e` (ellipse-centre offset), causing arcs to visually float away from the star in perspective mode where more orbits reach hd = 90° (full semicircle) due to compressed ry. Fix: use `abs(math.cos(incl_rad))` in all three functions; remove eccentricity offset — arcs now always centred on star_cx. Eccentricity still shown via compressed b_px y-radius. No new tests (SVG geometry). 1946 tests pass; pylint 10.00/10. |
| 2026-06-04 | Session 103 — Graceful JSON loading (issue #117). Version mismatch in gen-ui changed from `QMessageBox.critical()` hard abort to `QMessageBox.warning()` Yes/No dialog — user can choose to proceed. `from_dict()` methods hardened: `WorldDetail.from_dict()` (`d["sah"]` → `.get()`), `WorldPhysical.from_dict()` (9 bare constructor accesses → `.get()` with safe defaults), `BeltPhysical.from_dict()` (11 bare accesses → `.get()`), `Moon.from_dict()` (`d["size"]` → `.get()`). 4 new tests in `TestFromDictMissingFields`. Issue #113 closed (implemented in Session 88). 1946 tests pass; pylint 10.00/10. |
| 2026-06-04 | Session 102 — Body naming feature (issue #131). `attach_body_names(system)` added to `traveller_system_gen.py` (call after `attach_detail()`; deterministic; idempotent). `name: str` field added to `Star`, `OrbitSlot` (post-init), `Moon` (post-init), `WorldDetail` (`__slots__`). Naming scheme: non-companion stars → `<mw>-Primary/Secondary/…`; worlds → `<mw>-A/B/…`; belts → `<mw>-Belt-A/B/…`; moons → `<orbit>-alpha/beta/…` (rings skipped). Name column added as leftmost column in `system_card.html` and `system_detail.html`. `render_system_json.py` and `TravellerSystem.to_html()` both pass `"name"` in orbit and moon row dicts. Callers (`fastapi/app.py`, `gen-ui/app.py`, `azure-api/function_app.py`) wired. 12 new tests in `TestBodyNames`. 1942 tests pass; pylint 10.00/10. |
| 2026-06-04 | Session 101 — City population rounding to 3 significant figures. `_round_sig(n, sig=3)` helper added to `traveller_world_population_detail.py`; applied to `major_city_total_population` and each `City.population` before storage. `test_city_pops_within_total` tolerance widened from `+1` to `+0.5%` to accommodate independent rounding. GitHub issue #131 created for body naming feature. 1930 tests pass; pylint 10.00/10. |
| 2026-06-04 | Session 100 — Refactor: atmosphere-related code extracted from `traveller_world_physical.py` into new `traveller_world_atmosphere_detail.py`. Moved: `_ATM_*` constants, `_roll_albedo`, `_roll_greenhouse_factor`, `_MEAN_TEMP_TABLE_K`, `_MEAN_TEMP_ATM_DM`, `_orbit_dm_for_mean_temp`, `_compute_mean_temperature`, `_axial_tilt_factor`, `_rotation_factor`, `_geographic_factor`, `generate_advanced_mean_temperature`, `RunawayGreenhouseResult`, `check_runaway_greenhouse`. `generate_world_physical` lazy-imports `_compute_mean_temperature` from new module. `traveller_belt_physical` import updated. BeltPhysical guard uses `hasattr(physical, "density")` (no circular import). `_orbital_period_hours` duplicated as private copy. Import blocks updated in `gen-ui/app.py`, `fastapi/app.py`, `azure-api/function_app.py`, and `tests/test_world_physical.py`. `too-many-lines` suppression retained in `traveller_world_physical.py` (1102 lines). `context/atmosphere-detail.md` created. Bug fix: Biodiversity Rating formula corrected from `2D−7 + biomass + ⌈biocomplexity/2⌉` to `2D−7 + ⌈(biomass + biocomplexity)/2⌉` (WBH image formula). Two test values updated in `TestBiodiversityRating`. 1930 tests pass; pylint 10.00/10 on all changed files. |
|------|---------|
| 2026-04-14 | Session 1 — CRB mainworld generation, UWP, trade codes, JSON Schema, HTML card, Azure Functions (5 endpoints), test suite |
| 2026-04-17 | Session 2 — WBH stellar generation, orbit placement, system integration, orbital temperature, three orbit compliance bug fixes |
| 2026-04-18 | Session 3 — Secondary world SAH/social detail, moon generation quantity/sizing, moon SAH/social detail, compliance audit |
| 2026-04-19 | Session 4 — Belt population and mainworld fixes, moon size clamping, JSON serialisation of all detail data, `summary()` updates, four new system API endpoints, REST API documentation, GitHub prep |
| 2026-04-22 | Session 5 — Pylance fixes across all modules; orbit spread inner-system clustering bug fixed (min_spread floor + additive gap check); security hardening (`parse_hex_pos`, `ERR_INVALID_HEX`, `max_batch_size` bounds, upstream error scrubbing, CORS note); documentation updates; session context file created |
| 2026-04-23 | Session 6 — Created CLAUDE.md consolidating all .claude/context/ files; updated README generation rules section with full WBH coverage; refreshed backup archive |
| 2026-04-23 | Session 7 — Added `World.from_dict()` classmethod; added `generate_system_from_world()` entry point; added `parse_world_json()` helper; added `POST /api/system/from-world` endpoint (13th endpoint); created GTK4 UI skeleton in `gen-ui/app.py` |
| 2026-04-24 | Session 8 — GTK4 UI fully implemented: mainworld generation, HTML card auto-opened in system browser via `Gio.AppInfo.launch_default_for_uri`, save to JSON/text/HTML via `Gtk.FileDialog`; added `gen-ui/README.md` and `gen-ui/requirements.txt` |
| 2026-04-24 | Session 9 — Randomised seeding when no seed specified (`secrets.randbelow`) across API, generation modules, and gen-ui; `seed` field added to all JSON responses and JSON Schema; HTML hydrographics right-alignment fix; gen-ui system generation controls added |
| 2026-04-27 | Session 10 — gen-ui TravellerMap lookup wired (`generate_system_from_map`); `AmbiguousWorldError` added to `traveller_map_fetch.py`; disambiguation dialog in gen-ui (GitHub issues #1, #2 closed) |
| 2026-04-27 | Session 11 — gen-ui replaced browser-auto-open with native GTK4 in-app display; world card shows Physical/Society detail cards, stat boxes, trade code badges, notes; Full System checkbox wired to `generate_full_system()` |
| 2026-04-27 | Session 12 — Temperature compliance bug fixed: sub-Orbit#1 deviation scaling removed from `hz_deviation_to_raw_roll()` and `_temp_zone()`; pre-fix 58% frozen-in-HZ for dim stars → post-fix 0%; 502 tests pass |
| 2026-04-27 | Session 13 — Gas giant mainworld compliance bug fixed: `_gg_sah_roll()` inlined in `traveller_orbit_gen.py`, stored in `OrbitSlot.gg_sah`; gas giant branch added to `generate_mainworld_at_orbit()`; 10 new tests; 512 tests pass |
| 2026-04-29 | Session 14 — Created `system_map.py`: SVG star system map with per-star arc zones, log-AU radial scale, companion-star dashed arcs, orbit table zone; SVG-only output |
| 2026-04-30 | Session 15 — Ag trade code compliance fix (issue #4): corrected all three criteria, added population 5-7 check, six boundary tests. Added white background theme to system maps: `ColourPalette` dataclass, `PALETTE_DARK`/`PALETTE_LIGHT`, `--white-bg` flag. 523 tests pass. |
| 2026-04-30 | Session 16 — pip-installed `pycairo` and `PyGObject` into `.venv-1` (compiled against Homebrew GTK4 libs); added macOS DYLD re-exec block to `gen-ui/app.py` |
| 2026-04-30 | Session 17 — Migrated `gen-ui/app.py` from GTK4/PyGObject to PySide6 (Qt6): complete rewrite (~1100 lines); all generation logic unchanged; `QKeySequence::Quit/Close` handle cross-platform shortcuts automatically |
| 2026-04-30 | Session 18 — gen-ui bug fixes and layout polish: fixed `_on_source_toggled` early-return guard; fixed macOS SSL cert failure for TravellerMap; widened detail row value column; restructured source row layout |
| 2026-04-30 | Session 19 — Three Pylance fixes in `gen-ui/app.py`: `QApplication.instance()` narrowed via `isinstance`; `_clear_status` guards `takeAt()`/`widget()` against `None`; `orbit.world_type` cast to `str` via `str()` |
| 2026-04-30 | Session 20 — Pylance fixes in `traveller_orbit_gen.py` and `traveller_world_detail.py`: `OrbitSlot.detail` promoted to typed field; all `getattr` workarounds removed. Upgraded pip/setuptools (resolved 7 CVEs). Created `.github/workflows/dependency-audit.yml`. Updated `docs/developer-guide.md`. 523 tests pass. |
| 2026-04-30 | Session 21 — gen-ui `SystemMapWindow`: separate non-modal `QMainWindow` with `QSvgWidget`-rendered SVG, dark/light theme toggle, Save SVG button; "System Map" button added to system result header; SVG background fixed as explicit `<rect fill>` element; table zone wrapped in `<g font-family>` for Qt monospace. 523 tests pass. |
| 2026-05-05 | Session 22 — Pylint 10.00/10 pass across all six generation modules: docstrings, elif→flat-if, inline expansions, unused param rename, chained comparison fix, superfluous parens removed, `_size_gg_moon()` indentation bug fixed. 523 tests pass. |
| 2026-05-06 | Session 23 — New module `traveller_world_physical.py`: composition, density, diameter, mass/gravity/escape velocity, axial tilt (6-band table), basic rotation rate; `WorldPhysical` dataclass with `to_dict()`; `generate_world_physical(world, age_gyr)`. Updated `World.physical` field, `to_dict()`/`to_html()`/`summary()`. gen-ui: "Physical detail" checkbox, `_build_physical_card()`, stellar card shows system age. GitHub issues #10, #11 filed. 523 tests pass. |
| 2026-05-07 | Session 24 — Gas giant mainworld orbit display bug fixed (issue #22): three-layer fix across `attach_detail()`, `_orbit_profile()`, and `to_html()`; satellite appears as `moons[0]` sub-row; gas giant profile shown in orbit table. 523 tests pass. |
| 2026-05-07 | Session 25 — Implemented WBH p.77 Extreme Axial Tilt sub-table (issue #10): replaced placeholder in `_roll_extreme_axial_tilt()` with full 6-band 1D table producing 20–70° (high), 40–90° (extreme), 91–126° (retrograde), 144–179° (extreme retrograde), 130–180° (extreme retrograde high-variance). Updated module docstring. Deferred feature entry removed. 523 tests pass. |
| 2026-05-07 | Session 26 — Restructured AI context: CLAUDE.md rewritten as a short router (~70 lines); `context/` directory created with 13 topic files (common, generation-pipeline, data-structures, stellar-orbit, system-world, detail-moon, map-fetch, api-layer, system-map, gen-ui, compliance-audit, deferred-features, session-history). Content consolidated from CLAUDE.md and developer-guide.md; gaps fixed (OrbitSlot.gg_sah, WorldDetail.trade_codes, WorldPhysical added to data-structures). |
| 2026-05-08 | Session 27 — Tidal lock status implemented (WBH pp. 105–107, issue #11): 2D+DM table with 11 outcomes (no-effect, braking ×1.5–×5, prograde, retrograde, 3:2 lock, 1:1 lock); broken-lock check; axial tilt reroll for lock results; `WorldPhysical.tidal_status` field added; `generate_world_physical()` extended with `orbit_number`, `orbit_au`, `star_mass` optional params; gen-ui call site updated; `TIDAL_STATUS_LABELS` exported for display in `to_html()`/`summary()`/gen-ui card. 42 new tests; 565 tests pass. |
| 2026-05-08 | Session 28 — Belt physical detail implemented (WBH pp. 131–133, issue #19): new module `traveller_belt_physical.py` with `BeltPhysical` dataclass and `generate_belt_physical()`; `WorldDetail.physical` field added (`BeltPhysical | None`); `generate_system_detail()` computes span, composition, bulk, resource rating, and significant bodies for every belt orbit slot; `WorldDetail.to_dict()` includes `"physical"` key; 45 new tests; 610 tests pass. |
| 2026-05-08 | Session 29 — Belt physical detail for mainworld belts (issue #19 cont.): `attach_detail()` extended to generate `BeltPhysical` for belt mainworlds and set both `mainworld.physical` and `mainworld_orbit.detail.physical`; `World.physical` type widened to `Union[WorldPhysical, BeltPhysical]`; `World.to_html()` and `summary()` render belt card via `isinstance` dispatch; `TravellerSystem.to_html()` mainworld panel gains a belt body block; gen-ui `_build_physical_card()` dispatches on `isinstance(physical, BeltPhysical)`; 4 new tests; 614 tests pass. |
| 2026-05-08 | Session 30 — Two belt display bug fixes: (1) gen-ui `_build_stat_row()` accessed `physical.diameter_km`/`gravity` without BeltPhysical guard, crashing the system card and leaving only the header visible — fixed with `not isinstance(physical, BeltPhysical)` condition; (2) belt span formula was using AU range of system (`max_au − min_au`) instead of the WBH orbit generation spread `(max Orbit# − MAO) / n_orbits` — all belts in all systems were getting wildly large spans (e.g., 0–11.974 AU for a belt at 0.348 AU); corrected in both `generate_system_detail()` and `attach_detail()` mainworld block; issue #27 filed and closed. 614 tests pass. |
| 2026-05-11 | Session 31 — WBH atmosphere detail Phase 1 (pp. 78–82): new `AtmosphereDetail` dataclass on `World` with `pressure_bar`, `oxygen_partial_pressure`, `scale_height_km`. Helpers `_atmosphere_pressure_bar()`, `_oxygen_partial_pressure()`, `_scale_height_km()` in `traveller_world_gen.py`; `ATMOSPHERE_PRESSURE_SPAN_BAR` table for codes 1–9, D, E; `SIZE_GRAVITY_G` exported for the 8.5/g scale-height approximation; `_dice()` unclamped helper for the (2D-7)/100 variance term. New `generate_atmosphere_detail()` and `format_atmosphere_profile()` produce a WBH p.82 profile string. `World._atmosphere_dict()` adds nested `detail` and `profile` keys to JSON output when populated. Orchestrator (`generate_mainworld_at_orbit()`) now takes `system_age_gyr` and calls the detail generator after the atmosphere roll. Schema updated. 52 new tests; 640 tests pass. |
| 2026-05-12 | Session 32 — WBH atmosphere detail Phase 2 Stage 1 (pp. 82–85): taint generation for tainted atmosphere codes (2, 4, 7, 9). New `Taint` dataclass; constants `_TAINTED_CODES`, `_O2_TAINT_CODES`, `_BIOLOGIC_SUBTYPE_ROLLS`, `_TAINT_SUBTYPE_DM`, `_TAINT_SUBTYPE_TABLE`, `_TAINT_SEVERITY_TABLE`, `_TAINT_PERSISTENCE_TABLE`; helpers `_taint_severity_code()`, `_taint_persistence_code()`, `_roll_single_taint()`. `AtmosphereDetail` gains `taints: list`; `to_dict()` emits `"taints"` key when non-empty. Cascade mechanic: result 10 on subtype table (Particulates) triggers a second taint roll. `generate_atmosphere_detail()` gains reserved `temperature` parameter. Schema updated. 42 new tests; 682 tests pass. Also: `World.physical` renamed to `World.size_detail` throughout; JSON key `"physical"` → `"size_detail"`. Schema updated. Atmosphere detail coupling: gen-ui now ensures `atmosphere_detail` is set whenever size detail is generated. |
| 2026-05-12 | Session 32 cont. — Phase 2 Stage 2: `format_atmosphere_profile()` extended with `T.S.P` taint suffix (subtype code, severity code, persistence code per taint, e.g. `7-1.148-0.138-P.7.9`). gen-ui atmosphere card now displays each taint (subtype, severity, persistence). 8 new tests; 690 tests pass. |
| 2026-05-12 | Session 32 cont. — Phase 2 Stage 3: `World.to_html()` gains an Atmosphere detail inner card (profile, pressure, O₂, scale height, taints with severity/persistence); `World.summary()` gains an atmosphere detail section. Issue #29 closed. 18 new tests; 708 tests pass. |
| 2026-05-12 | Session 33 — Phase 3 Stage 1 (pipeline reorder): in `generate_mainworld_at_orbit()` both the gas-giant and terrestrial branches now call `generate_temperature_from_orbit()` before `generate_atmosphere_detail()`, passing `world.temperature` as the `temperature` parameter. `generate_system_from_world()` already had the correct order. Seed-breaking change (accepted). 708 tests pass. |
| 2026-05-12 | Session 33 cont. — Phase 3 Stage 2: exotic/corrosive/insidious atmosphere subtypes and insidious hazards (WBH pp.85-87). New `InsidiousHazard` dataclass; `_EXOTIC_SUBTYPE_TABLE`, `_CI_SUBTYPE_TABLE`, `_INSIDIOUS_HAZARD_TABLE`, `_HAZARDOUS_GASES` constants; `_roll_exotic_subtype()`, `_roll_ci_subtype()`, `_roll_insidious_hazard()`, `_subtype_pressure_bar()` helpers. `AtmosphereDetail` gains `subtype_code`, `subtype_name`, `hazards` fields. `hz_deviation` parameter added to `generate_atmosphere_detail()` and all call sites. Unbound pressure (subtype C/D/E) stored as `None`; displayed as "> 10.0 bar". `World.to_html()`, `summary()`, gen-ui card updated to show subtype and hazards. Schema updated. 33 new tests; 743 tests pass. |
| 2026-05-12 | Session 34 — Phase 4: Gas mix generation (WBH pp.87–95). New `GasMixComponent` dataclass; `_GAS_CODES` dict (24 gases); 7 gas-mix table constants (Boiling-VH, Boiling-H, Hot, Temperate, Cold, Frozen-M, Frozen-D); helpers `_select_gas_mix_table()`, `_roll_single_gas()`, `_roll_gas_mix()`; public `generate_gas_mix()` mutates `AtmosphereDetail.gas_mix` after hydrographics. CO* substitution: CO→CO₂ (non-frozen, hydro>0) or CO→N₂ (frozen, hydro>0). `format_atmosphere_profile()` appends `:code-##` gas tokens. `to_html()`, `summary()`, gen-ui atmosphere card show gas mix. Three call sites added to `traveller_system_gen.py` (after hydrographics). Schema updated. GitHub issue #44 filed (atmospheric retention check). 62 new tests; 805 tests pass. |
| 2026-05-13 | Session 34 cont. — gen-ui atmosphere detail call-site fixes: imported `generate_gas_mix`; added `temperature=world.temperature` to both `generate_atmosphere_detail` call sites in `_finish_generation` and `_finish_system_generation`; added `generate_gas_mix` calls after each (inside the `atmosphere_detail is None` guard so pipeline-generated worlds are not re-rolled). Standalone worlds now populate gas mix and use temperature for Phase 3 subtype DMs. 805 tests pass. |
| 2026-05-13 | Session 34 cont. — gen-ui atmosphere detail always visible: decoupled `atmosphere_detail` generation from the Physical detail checkbox in `_finish_generation`. Atmosphere detail (Phases 1–4) is now generated unconditionally for standalone worlds; only the World Body card (`size_detail`) remains gated on the Physical checkbox. TravellerMap and full-system paths unaffected (pipeline sets `atmosphere_detail` before `_finish_generation` runs). 805 tests pass. |
| 2026-05-13 | Session 35 — Phase 5: altitude bands and Unusual (F) atmosphere subtypes (WBH pp.90–93). Stage 1: `_compute_very_dense_altitude()` and `_compute_low_altitude()` helpers using Bad Ratio formula; `AtmosphereDetail` gains `min_safe_altitude_km` and `no_safe_altitude` fields; optional taint roll (1D≥4) for codes 13/14 added to `generate_atmosphere_detail()`. Stage 2: `UnusualSubtype` dataclass; `_UNUSUAL_SUBTYPE_TABLE` (D26, 12 subtypes); `_d26()`, `_roll_unusual_subtype()` helpers with Layered/Panthalassic/Steam prerequisite checks; public `generate_unusual_subtype()` mutates `AtmosphereDetail.unusual_subtypes`; `format_atmosphere_profile()` short-circuit for code 15 (`F-S{codes}`); `AtmosphereDetail` gains `unusual_subtypes` field. Stage 3: `to_html()`, `summary()`, and gen-ui atmosphere card show altitude and unusual subtype rows. Three call sites in `traveller_system_gen.py`, four + batch in `function_app.py`, two in `gen-ui/app.py`. Schema updated with three new properties. `import math` added. ~58 new tests; 890 tests pass. |
| 2026-05-13 | Session 36 — Bug fixes (issue #51): (1) `TravellerSystem.to_html()` mainworld panel now renders `WorldPhysical` and atmosphere detail inner-cards (was omitting both; only `BeltPhysical` was handled). Added `.inner-card`, `.inner-lbl`, `.drow`, `.dlbl` CSS and a `drow()` helper to the system HTML template; imports `WorldPhysical`, `TIDAL_STATUS_LABELS`, `format_atmosphere_profile`. (2) `generate_system_from_map()` in `traveller_map_fetch.py` now calls `generate_gas_mix()` and `generate_unusual_subtype()` after `generate_atmosphere_detail()`, completing the atmosphere pipeline for TravellerMap-fetched worlds; `hz_deviation` now threaded into `generate_atmosphere_detail()` for orbit-position DMs. 890 tests pass; pylint 10.00/10 on both modified files. |
| 2026-05-15 | Session 37 — Install scripts: `install.bat` committed; `install.ps1` and `install.sh` gained Python auto-install (winget on Windows; brew/apt/dnf/pacman on macOS/Linux). New module `traveller_hydro_detail.py`: `HydrographicDetail` dataclass, `generate_hydrographic_detail()`, WBH p.93 surface liquid percentage (flat random over code ranges); wired into `traveller_system_gen.py` (shared section), `function_app.py` (4 handlers + batch), `traveller_map_fetch.py`, and both gen-ui `_finish_generation` paths. New `_build_hydrographic_card()` in gen-ui. Hydrographic Detail inner-card added to `World.to_html()`, `TravellerSystem.to_html()`, and `World.summary()`. JSON: `hydrographics.detail.surface_liquid_pct` nested under `"detail"` key. Schema updated. `# pylint: disable=too-many-lines` added to `traveller_system_gen.py`. `test_function_app.py` updated for pipeline seed shift. 29 new tests in `tests/test_hydro_detail.py`; 919 tests pass. |
| 2026-05-15 | Session 37 cont. — Bug fix #55 (H/L oxygen taint ppo validation): `_roll_single_taint()` gained `ppo: Optional[float]` parameter; High Oxygen (H) now rerolled unless ppo > 0.5 bar; Low Oxygen (L) now rerolled unless ppo < 0.1 bar. ppo computation moved before the taint block in `generate_atmosphere_detail()` so it is available at taint time (seed-breaking for tainted codes 2, 4, 7, 9, 13, 14). Fixed one hanging test (`test_taint_fires_for_code_14` — infinite loop from H taint blocked by the new guard). Two existing tests updated to pass ppo; 6 new tests in `TestTaintPpoValidation`. 925 tests pass. |
| 2026-05-15 | Session 38 — NHZ atmosphere generation (WBH pp. 78–79). New atmosphere codes 16 (Gas, Helium / G) and 17 (Gas, Hydrogen / H) added to `ATMOSPHERE_NAMES`, `ATMOSPHERE_GEAR`, `TEMPERATURE_DM`. Four NHZ table constants: `_NHZ_HOT_A` (HZCO ≤ -2.01), `_NHZ_HOT_B` (-1.01 to -2.0), `_NHZ_COLD_A` (+1.01 to +3.0), `_NHZ_COLD_B` (≥ +3.01); each maps 2D-7+Size results to `(atm_code, base_exotic_key, irritant_exotic_key, star, dagger)` tuples. New `generate_nhz_atmosphere(size, hz_deviation)` returns `(atm_code, exotic_key)`. `generate_atmosphere_detail()` gains `exotic_key_override` parameter (bypasses `_roll_exotic_subtype()` for NHZ exotic worlds) and early-return for codes 16/17. `generate_hydrographics()` returns 0 for codes 16/17. `format_atmosphere_profile()` returns `to_hex(code)` for codes 16/17. `generate_full_system()` and `generate_mainworld_at_orbit()` gain `nhz_atmospheres: bool = False`; NHZ branch fires when `abs(hz_deviation) > 1.0`. CLI gains `--nhz-atmospheres` flag. gen-ui: "NHZ Atmospheres" checkbox added (enabled only when System detail is checked). JSON schema `atmosphere.code.maximum` updated 15→17. 33 new tests; 952 tests pass. |
| 2026-05-15 | Session 38 cont. — NHZ atmospheres extended to secondary worlds and moons. `TravellerSystem` gains `nhz_atmospheres: bool = False` field (set by `generate_full_system()`). `_terrestrial_sah()`, `_moon_detail()`, and `generate_system_detail()` each gain `nhz_atmospheres: bool = False` parameter. `attach_detail()` reads `system.nhz_atmospheres` and threads it through all secondary-world and moon generation paths. `generate_nhz_atmosphere` added to `traveller_world_detail.py` imports. No changes to `gen-ui/app.py` or `function_app.py` — flag travels via `system.nhz_atmospheres`. 5 new tests in `TestNhzSecondaryWorlds`; 957 tests pass. |
| 2026-05-18 | Session 39 — Companion star exclusion zone bug fixed in `traveller_orbit_gen.py`. When companion orbit# < 1.0, `excl = companion_orbit - 1.0` was ≤ 0 and never triggered the `max_o` cap, allowing primary worlds inside the WBH exclusion zone [companion−1, companion+3]. Fix: `else` branch pushes `mao = max(mao, companion_orbit + 3.0)` and syncs `star_mao[designation]`. Also: `system_map.py` extended to render companion star rows inside the primary star's orbit table section, sorted by orbit number. 2 new tests in `TestCompanionExclusionZone`; 959 tests pass. |
| 2026-05-18 | Session 39 cont. — Primary star outer zone placement implemented in `traveller_orbit_gen.py`. Primary stars with a valid inner zone (companion−1 > MAO) now also populate the outer zone [companion+3, 17]. New `star_outer` dict tracks outer zone bounds; `_avail_range()` nested helper includes outer range in proportional world allocation; placement loop wraps in a `for zone in zones` loop that runs once per zone (inner + outer). 4 new tests in `TestPrimaryOuterZone`; 963 tests pass. |
| 2026-05-18 | Session 39 cont. — `system_map.py` orbit table gains a column label sub-header row (`#  Orbit#  AU  Type  Profile  Codes  Zone ♦`). `Zone ♦` makes explicit that the last column shows both temperature zone and moon count. `_TBL_ROW0_OFF` bumped 38→50 to accommodate the new row. |
| 2026-05-18 | Session 40 — Companion star orbit periods implemented. `Star` gains `orbit_period_yr: Optional[float]` field; computed in `generate_stellar_data()` using `√(AU³/(M+m))`. M_central for companions = parent mass; for secondaries = combined mass of all stars with effective system orbit# < this star's orbit#. `_eff_sysorn()` nested helper handles companions-of-secondaries correctly (e.g. Ba's effective system orbit = B's orbit#, so Ca's M includes Ba). `Star.to_dict()` and `StarSystem.summary()` include the period. `system_map.py`: new `_C_PERIOD = 490` column, `_fmt_period()` helper (h/d/y auto-scaling), period shown in companion/secondary star rows and non-primary column headers. No new dice rolls; no seed disruption. 963 tests pass. |
| 2026-05-18 | Session 41 — Anomalous orbits implemented (WBH pp.49-50, issue #12). `OrbitSlot` gains `anomaly_type: str` field. `generate_orbits()` now rolls for anomalous orbit count (2D: ≤9 → 0, 10 → 1, 11 → 2, 12 → 3) and type (random, eccentric, inclined, retrograde, trojan). Random/eccentric/inclined/retrograde use a 2D-2 + d10 fractional roll constrained to the star's valid zone (respecting companion exclusion bands with ±0.01 clearance from boundaries). Trojan orbits pick an existing non-empty host slot and co-occupy its orbit#. Inclined orbits roll (1D+2)×10 + d10 degrees. `OrbitSlot.to_dict()` emits `anomaly_type` when set. `system_map.py`: anomaly indicator appended to type column (e.g., "terr R" for retrograde, "terr L4" for trojan_leading). `TravellerSystem.to_html()` and `SystemOrbits.summary()` show anomaly notes in the notes column. Seed-breaking for all systems (new 2D roll fires every generation). 6 new tests in `TestAnomalousOrbits`; 969 tests pass. |
| 2026-05-18 | Session 40 cont. — World orbital periods implemented. `OrbitSlot` gains `orbit_period_yr: Optional[float]` (`init=False`); computed in `generate_orbits()` after slot placement using `√(AU³/M_central)`. M_central = designated star mass + companions whose orbit_au < world orbit_au. Planet mass correction (WBH: mE×0.000003) omitted as negligible. `OrbitSlot.to_dict()` emits `orbit_period_yr`. `system_map.py` Period column now populated for world rows. `gen-ui/app.py`: `_fmt_period()` promoted to module level; System Orbits card gains Period column in both detail/non-detail variants. No new dice rolls; 963 tests pass. |
| 2026-05-18 | Session 41 cont. — Notes column added to gen-ui System Orbits card and `TravellerSystem.to_html()` orbit table. `_build_orbits_card()` now has 10 columns (detail_attached) and 8 columns (non-attached), both with a trailing Notes column. `to_html()` orbit table now uses a `note_parts` list combining the "← mainworld" marker with `OrbitSlot.notes`, showing all notes unconditionally (anomaly notes, HZ notes, etc.). No new tests; 969 tests pass. |
| 2026-05-18 | Session 42 — Bug fix #52 (incorrect belt counts for fetched mainworlds). Two bugs in `traveller_map_fetch.py`: (1) `_reconcile_orbit_types()` now promotes empty orbit slots when canonical_gg + canonical_belt exceeds available non-empty non-mainworld slots, so belt/GG counts are always honoured. Previously belts were silently dropped because the pool was truncated before shuffle. (2) `generate_system_from_map()` now subtracts 1 from `canonical_belt` when the mainworld is a belt (size==0), because WBH convention (confirmed in `generate_belt_count()`) includes the mainworld belt in the PBG B digit. 4 new tests in `TestReconcileOrbitTypes`; 973 tests pass. |
| 2026-05-19 | Session 46 — Orbital inclination implemented (WBH p.28, issue #59). Optional feature gated on `orbital_inclination=False` flag (same pattern as `orbital_eccentricity`). `Star` gains `orbit_inclination: float = 0.0`; `OrbitSlot` gains `inclination: float = field(default=0.0, init=False)`. `_roll_inclination()` added to `traveller_orbit_gen.py` using 6-row severity table (Very Low/Low/Moderate/High/Very High/Extreme) with recursive retrograde case (2D=12 → 180 − sub-roll). Post-placement pass in `generate_orbits()` sets inclination for all non-empty, non-anomalous-inclined slots when flag is set; anomalous "inclined" orbits skipped (angle already in notes). `to_dict()` emits `inclination` when > 0. `TravellerSystem` stores `orbital_inclination` flag. Flag plumbed through `generate_full_system()`, `generate_system_from_world()`, and all 5 system API endpoints via new `parse_orbital_inclination()` helper. gen-ui and HTML orbit table: "Ecc" column renamed "Ecc/Incl"; cell shows `ecc/incl°` combined, falling back to "—" when both zero. gen-ui gains "Orbital Inclination" checkbox (enabled only when "Full system" is checked). No seed disruption when flag is False. 8 new tests in new `tests/test_orbit_gen.py`; 998 tests pass. |
| 2026-05-19 | Session 45 — Three maintenance fixes. (1) JSON Unicode error in tests (issue #60): `open(schema_path)` in `tests/test_traveller_world_gen.py` lacked `encoding="utf-8"`, causing failures on Windows where the default codepage (cp1252) cannot decode non-ASCII characters in `traveller_world_schema.json`. Fixed to `open(schema_path, encoding="utf-8")`. (2) Virtual environment consolidated (issues #61 and #62): dual `.venv` + `.venv-1` setup replaced by a single `.venv`; install scripts (`install.sh`, `install.ps1`, `install.bat`) now install all dependencies in one pass from `requirements.txt`, `gen-ui/requirements.txt`, and new `requirements-dev.txt` (pytest/pylint), then activate the venv. All `.vscode/settings.json`, docs, and gen-ui references updated from `.venv-1` to `.venv`. |
| 2026-05-18 | Session 44 cont. — Dedicated `Ecc` column added to gen-ui System Orbits card and `TravellerSystem.to_html()` orbit table (replaces the former inline `(e=X.XXX)` embedded in the AU cell). Both header variants gain a right-aligned `"Ecc"` column inserted after `"AU"`: detail_attached is now 11 columns (`right_cols={1,2,3,9}`); non-attached is 9 columns (`right_cols={1,2,3,7}`). AU cells now show bare `orbit_au:.3f`. Moon sub-row `colspan` in `to_html()` widened from 3 → 4. Separate fix: added `_check_ecc` ("Orbital Eccentricity") checkbox to gen-ui, enabled/disabled alongside "NHZ Atmospheres" when "System detail" is toggled; passes `orbital_eccentricity=self._check_ecc.isChecked()` to `generate_full_system()`. Previously the flag defaulted to False on every call, so all `OrbitSlot.eccentricity` values were 0.0 and the Ecc column always showed "—". |
| 2026-05-18 | Session 44 — 1:1 tidal lock axial tilt and eccentricity interactions implemented (WBH p.77, issue #10). Two compliance bugs fixed in `traveller_world_physical.py`: (1) Rule 3: `_apply_tidal_lock_result()` 1:1 case now calls new `_roll_axial_tilt_1d()` unconditionally (1D selects outer band on Axial Tilt table, inner 1D gives degrees within band) instead of the incorrect `_reroll_axial_tilt_for_lock()` + `> 3.0` guard. (2) Rule 4: `_reroll_eccentricity_tidal()` rolls eccentricity with DM-2 for 1:1 locked worlds; `generate_world_physical()` gains `orbit_eccentricity: float = 0.0` parameter; when `tidal_status == "1:1_lock"` and `orbit_eccentricity > 0.1`, the lower of original/new eccentricity is stored in new `WorldPhysical.eccentricity_adjusted` field. `function_app._attach_mainworld_physical()` threads the orbit slot eccentricity in and propagates any reduction back to the orbit slot. `_ECC_TABLE_PHYS` inlined to avoid circular import. 3:2 lock axial tilt unchanged. 11 new tests; 990 tests pass. |
| 2026-05-19 | Session 48 — Bug fix #64: anomalous orbit eccentricity DMs not applied (WBH pp.49-50). Random/eccentric/inclined/retrograde anomalous orbits must apply DMs +2/+5/+2/+2 when rolling on the Eccentricity Values table; trojan types have no DM. Added `_ANOM_ECC_DM` dict after `_ECC_TABLE`; added `anomaly_dm: int = 0` parameter to `_roll_eccentricity()` (+ pylint suppress `too-many-arguments,too-many-positional-arguments`); eccentricity pass in `generate_orbits()` now passes `anomaly_dm=_ANOM_ECC_DM.get(o.anomaly_type, 0)`. No seed disruption for systems without anomalous orbits. 6 new tests in `TestAnomalyEccentricityDMs`; 1008 tests pass. |
| 2026-05-19 | Session 50 — Tidal lock eccentricity DM implemented (WBH p.105). `_tidal_lock_dm()` gains `orbit_eccentricity: float = 0.0` parameter; when `e > 0.1` applies `DM − floor(e × 10)` (general DM, all cases). Threaded through `_roll_tidal_lock_status()` and `generate_world_physical()` (already had `orbit_eccentricity` parameter from Session 44). No seed disruption when `orbital_eccentricity=False` (default). Stale "Orbital eccentricity" row removed from `context/deferred-features.md` (feature fully implemented in Sessions 43 + 48). 6 new tests in `TestTidalLockEccentricityDm`; 1014 tests pass. |
| 2026-05-19 | Session 52 — Moon quantity adjacency DMs implemented (WBH p.56, issue #14). Three previously deferred DM conditions now applied in `_moon_quantity()`: companion star exclusion zones (orbit# ±1 to +3), host star MAO boundary (±1.0), outermost Far-star slot proximity (±1.0). New `_moon_adjacency_context()` helper in `traveller_world_detail.py` computes context from system structure. `generate_moons()` and `_moons_for()` gain three new optional params; both `generate_system_detail()` and `attach_detail()` call sites pass context. 8 new tests in `TestMoonQuantityAdjacencyDMs`; 1044 tests pass. |
| 2026-05-19 | Session 52 cont. — Moon tidal lock DMs and planet-to-moon lock implemented (WBH pp.106–107, issue #11). `_tidal_lock_dm()` gains moon-size DM (DM − total size of significant moons) and multi-star DM (DM − stars orbited, defaulting to 1). New `_planet_moon_lock_dm(moon, all_moons)` implements WBH p.107 DM table (base −10, size DM, PD-range DMs, multiple-moons penalty). `_roll_tidal_lock_status()` now assembles star + moon candidates sorted by highest DM (moon before star on tie) and cascades until a lock fires. New public `apply_moon_tidal_effects()` resolves circular dependency (WorldPhysical needs moons; moons need WorldPhysical) via three-phase pipeline: `generate_world_physical()` → moon gen → `apply_moon_tidal_effects()`. `function_app.py`: `_get_mainworld_moons()` + `_apply_mainworld_moon_tidal()` helpers wired into 6 call sites. `gen-ui/app.py`: inline moon extraction and call in `_finish_system_generation()`. 24 new tests across 4 new test classes; 1068 tests pass. |
| 2026-05-19 | Session 51 — Moon orbit placement implemented (WBH pp.74–77, issue #16). `traveller_moon_gen.py`: `Moon` dataclass gains 8 new fields (`orbit_pd`, `orbit_km`, `orbit_range`, `orbit_period_hours`, `ring_centre_pd`, `ring_span_pd`, `orbit_eccentricity`, `orbit_retrograde`). New helpers: `_hill_sphere_au`, `_hill_sphere_pd`, `_hill_moon_limit`, `_moon_orbit_range`, `_roll_moon_pd`, `_moon_period_hours`, `_estimate_diameter_km`, `_estimate_mass_earth`, `_assign_sig_moon_orbits`, `_place_ring`. `generate_moons()` gains 5 new optional parameters (`planet_diameter_km`, `planet_mass_earth`, `orbit_au`, `star_mass_solar`, `planet_ecc`); when `orbit_au > 0` and `star_mass_solar > 0` the Hill sphere is computed, moons removed/converted if too close, MOR computed, orbit PDs rolled and sorted, ring centre/span placed. `traveller_world_detail.py`: `_moons_for()` gains `orbit_au`, `planet_ecc`, `star_mass_solar` parameters and threads them through; moon attachment loop in `generate_system_detail()` looks up host star and passes orbit data; `attach_detail()` passes `WorldPhysical.diameter_km` and `WorldPhysical.mass` for mainworld (via `mainworld.size_detail`). Moon orbit eccentricity/direction deferred. 22 new tests in new `tests/test_moon_gen.py`; 1036 tests pass. |
| 2026-05-19 | Session 49 — New standalone script `render_system_json.py`: reads a system JSON file produced by `TravellerSystem.to_dict()` and renders a rich self-contained HTML document; no project module imports (stdlib only: `json`, `sys`, `html`, `pathlib`). Renders system header, stars table, habitable zones, world-count chips, orbital survey (with moon sub-rows), mainworld panel (stats grid, trade badges, WorldPhysical/BeltPhysical card, atmosphere detail card, hydrographic detail card), and raw JSON collapsible. Distinguishes `WorldPhysical` vs `BeltPhysical` by checking for `"composition"` key. Pylint 10.00/10. JSON Schema fix: `eccentricity_adjusted` added to `WorldPhysical` branch of `traveller_world_schema.json`; the field was emitted by `WorldPhysical.to_dict()` but absent from the schema, causing `jsonschema.validate()` failures for 1:1 tidally-locked worlds with `orbit_eccentricity > 0.1`. Sample output: `examples/system_seed42.json` and `examples/system_seed42.html` (seed=42, 3-star system). 1008 tests pass. |
| 2026-05-19 | Session 47 — Bug fix #63: `orbital_eccentricity` and `orbital_inclination` flags not wired through TravellerMap endpoints. Three-layer gap: `generate_system_from_map()` did not accept the flags, `_map_system_response()` had no `want_ecc`/`want_incl` params, and neither endpoint handler called the parse helpers. All three layers fixed; flags thread into `generate_orbits()` and stored on `TravellerSystem`. Pylint suppress `too-many-arguments,too-many-locals,too-many-positional-arguments` added to `generate_system_from_map()`. 4 regression tests in `TestGenerateSystemFromMapOrbitalFlags`; 1002 tests pass. |
| 2026-05-18 | Session 43 — Orbital eccentricity implemented (WBH p.27, issue #57). Optional feature gated on `orbital_eccentricity=False` flag. `Star` gains `orbit_eccentricity: float = 0.0`; `OrbitSlot` gains `eccentricity: float = field(default=0.0, init=False)`. `_ECC_TABLE` and `_roll_eccentricity()` added to `traveller_orbit_gen.py`; post-placement pass in `generate_orbits()` computes world/belt/star eccentricities when flag is set. DMs: +2 for stars, +1 per extra star orbited beyond the first, −1 for orbit# < 1.0 if age > 1 Gyr, +1 for belts. `to_dict()` emits `eccentricity`, `orbit_au_min`, `orbit_au_max` when non-zero. `TravellerSystem` stores `orbital_eccentricity` flag. Flag plumbed through `generate_full_system()`, `generate_system_from_world()`, and all API endpoints via new `parse_orbital_eccentricity()` helper. gen-ui and system_map show `e=X.XXX` inline in AU column when eccentricity > 0. No seed disruption when flag is False. 6 new tests in `TestOrbitalEccentricity`; 979 tests pass. |
| 2026-05-20 | Session 56 — Seismic stress implemented (WBH Seismology ~pp.125-128). `traveller_world_physical.py`: new helpers `_compute_rss()` (Residual Seismic Stress: `floor(Size - Age + DMs)²`, DMs for is_moon/moon sizes/density), `_compute_thf()` (Tidal Heating Factor: `PrimaryMass⊕² × (diam/1600)⁵ × e² / (3000 × dist_Mkm⁵ × period_days × WorldMass⊕)`, values < 1 = 0), `_apply_seismic_stress()` (sets all four new fields on WorldPhysical, computes seismic temperature). `WorldPhysical` gains four new `Optional[int]` fields: `residual_seismic_stress`, `tidal_heating_factor`, `total_seismic_stress`, `seismic_temperature_k` (adjusted temperature via `⁴√(T⁴+TSS⁴)`, only when value differs from mean_temperature_k). `apply_moon_tidal_effects()` gains `is_moon: bool = False` parameter; early return for empty moons removed (tidal lock re-run skipped for empty moons but seismic stress always computed). `function_app._apply_mainworld_moon_tidal()` detects `is_moon` via `mw_orbit.world_type == "gas_giant"`, removes guard against no moons. `gen-ui/app.py` call site and `_build_physical_card()` updated to show RSS, THF (when >0), TSS, seismic temperature. `templates/world_card.html` and `render_system_json.py._phys_rows()` updated. `traveller_world_schema.json` gains four new optional fields. Tidal Stress Factor (WBH p.126) deferred as issue #67. 22 new tests; 1107 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-21 | Session 59 — Two fixes from WBH p.125 review. (1) Rename: `tidal_heating_factor` → `tidal_seismic_stress` throughout (field, JSON key, schema, gen-ui label, world_card.html, render_system_json.py, tests). Private function `_compute_thf()` renamed `_compute_tidal_ss()`. Formula unchanged; terminology now matches WBH. (2) Bug fix: `apply_moon_tidal_effects()` crashed with `AttributeError` when called for a Size 0 (belt) mainworld — `BeltPhysical` has no `density`, `axial_tilt`, or seismic fields. Fixed with `isinstance(physical, WorldPhysical)` early-return guard. 1189 tests pass; pylint 10.00/10. |
| 2026-05-21 | Session 58 — Surface tidal amplitude implemented (WBH pp.107–108, issue #68). `traveller_world_physical.py`: new helpers `_moon_mass_earth(size)` (Terran-density estimate: `(size×1600/12742)³`), `_star_tidal_effect_m(star_mass_solar, size, orbit_au)` (`star_mass×size / (32×AU³)`), `_moon_tidal_effect_m(moon, planet_size)` (`moon_mass×size / (3.2×(orbit_km/1e6)³)`, excludes rings and moons with no orbit_km), `_compute_tidal_amplitude(star_mass, size, orbit_au, moons)`. `WorldPhysical` gains `tidal_amplitude_m: Optional[float]`; `generate_world_physical()` sets star-only preliminary value; `apply_moon_tidal_effects()` adds moon contributions and updates in-place. `to_dict()` emits `tidal_amplitude_m` (4 dp) when set. `traveller_world_schema.json` gains `tidal_amplitude_m` in WorldPhysical branch. Display updated in gen-ui `_build_physical_card()`, `templates/world_card.html`, and `render_system_json.py`. Also fixed: pre-existing `_ZONE_OBJECT_NAME → ZONE_CSS_CLASS` name typo in gen-ui; `test_moon_lock_occurs_when_dm_high_enough` patched deterministic (broken-lock check caused intermittent flap). 21 new tests in 5 new test classes; 1189 tests pass; pylint 10.00/10. |
| 2026-05-21 | Session 57 — Enum types (issue #38) and centralised display tables (issue #39). New `world_codes.py`: `StarportCode`, `TemperatureCategory`, `TradeCode`, `TravelZone` (`StrEnum`); `AtmosphereCode` (`IntEnum`). `parse_uwp()` now raises `ValueError` (with chained context) for wrong length, missing separator, unrecognised starport, or non-hex code position. New `World._validate_world_codes()` static method validates starport, atmosphere, temperature, trade codes, travel zone, and all integer range fields in `from_dict()`. New `tables.py`: single canonical source for 7 display-layer lookup tables (`SIZE_DIAMETER_LABEL`, `SIZE_GRAVITY_LABEL`, `POPULATION_RANGE`, `TRADE_CODE_FULL`, `BASE_FULL`, `ZONE_CSS_CLASS`, `TIDAL_STATUS_LABELS`); all five consumers updated. Pyright CI: `.github/workflows/typecheck.yml` added (runs on every push/PR to `main`); `pyrightconfig.json` updated; `requirements-dev.txt` gains `pyright>=1.1.0`; 8 pre-existing Pyright errors fixed (stored-boolean narrowing → `isinstance`, `Moon` forward reference via `TYPE_CHECKING`, `hasattr` → `isinstance`). `.pylintrc` restored after accidental deletion in f9f6ca1; `duplicate-code` and `cyclic-import` suppressions documented. 1168 tests pass; pylint 10.00/10 maintained. |
| 2026-05-22 | Session 60 cont. — Tidal Stress Factor implemented (WBH p.126, issue #67). `WorldPhysical` gains `tidal_stress_factor: Optional[int]` = `floor(tidal_amplitude_m / 10)`. Computed inside `_apply_seismic_stress()` via `_compute_tidal_amplitude()` (already implemented in Session 58); `tidal_amplitude_m` assignment moved here from `apply_moon_tidal_effects()` caller. `total_seismic_stress` now = RSS + Tidal Seismic Stress + TSF. `to_dict()` and JSON schema updated; display added to gen-ui, `world_card.html`, `render_system_json.py` (order: tidal_seismic_stress → TSF → RSS → total). Seismic display order also corrected this session (was RSS → tidal → total). 6 new tests; 1195 tests pass; pylint 10.00/10. |
| 2026-05-22 | Session 60 — gen-ui issue #58: removed "Orbital Eccentricity" and "Orbital Inclination" checkboxes from `gen-ui/app.py`. Both flags are now always `True` when "System detail" is enabled. Updated all three call sites (`_on_generate`, `_do_travellermap_generation`, `_show_disambiguation_dialog`). `context/gen-ui.md` updated. No tests affected. |
| 2026-05-20 | Session 55 — Bug A fix (issue #66): removed superseded `hz_deviation_to_raw_roll()` call from orbital context note in `traveller_system_gen.py`; note now shows orbit#, AU, HZ deviation only. Verified all three p.108 Basic Mean Temperature rules remain correctly implemented. v1.2-new-features.txt created. Context updated for v1.3.0. |
| 2026-05-20 | Session 54 — Basic Mean Temperature implemented (WBH p.47). `traveller_world_physical.py`: new `_MEAN_TEMP_TABLE_K` dict (rolls 0–12, K values authoritative), `_MEAN_TEMP_ATM_DM` inline dict (copy of `TEMPERATURE_DM` to avoid circular import), `_orbit_dm_for_mean_temp(hz_deviation)` (DM+4+1/0.5 inside HZCO-1; DM-4-1/0.5 outside HZCO+1; 0 in HZ), `_compute_mean_temperature(hz_deviation, atmosphere)` (modified roll = 7 + orbit_dm + atm_dm; table lookup; -5K/step below 0; +50K/step above 12; minimum 3K). `WorldPhysical` gains `mean_temperature_k: Optional[int] = field(default=None, init=False)`; `to_dict()` emits `mean_temperature_k` when set. `generate_world_physical()` gains `hz_deviation: Optional[float] = None` parameter; sets `wp.mean_temperature_k` when provided. Both call sites updated: `function_app._attach_mainworld_physical()` and `gen-ui/app.py._finish_system_generation()` now pass `hz_deviation`. All four HTML templates (`world_card.html`, `world_list.html`, `system_card.html`) gain a Mean Temperature row after Day length. `render_system_json.py._phys_rows()` emits the field when present. `traveller_world_schema.json`: `mean_temperature_k` added to WorldPhysical. Pre-existing unused imports removed from `test_world_physical.py`; `apply_moon_tidal_effects()` pylint suppress added; two pre-existing long lines in `gen-ui/app.py` fixed. 17 new tests; 1085 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-20 | Session 53 — HTML template refactor (issue #40). Added `Jinja2>=3.1.0` to `requirements.txt`. New `html_render.py` with module-level Jinja2 environment (`autoescape=True`). New templates: `world_card.html`, `world_list.html`, `system_card.html`. `World.to_html()` refactored from ~350 lines of f-string HTML to a 3-line template call; new `_world_html_ctx(world)` module-level helper pre-computes all display values. `TravellerSystem.to_html()` similarly refactored from ~400 lines to template delegation. Multi-world CLI `--html` output fixed: replaced fragile regex stitching with `world_list.html` template. README updated with install script guidance for all three platforms (macOS/Linux, Windows PowerShell, Windows Command Prompt). `render_system_json.py` refactored to use new `system_detail.html` template (richer than `system_card.html`: adds Period column, Notes column, HZ zones section, orbit-count chips); all `_render_*` HTML-string functions replaced with data-builder functions returning plain dicts. Mainworld notes (`World.notes`) added to `system_card.html` and `mw_data` in `TravellerSystem.to_html()` — previously missing from system HTML output (visible in gen-ui but absent from HTML); `system_detail.html` already had notes from the initial implementation. 1068 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-22 | Session 61 — Biomass Rating implemented (WBH pp.127-131). New `generate_biomass_rating()` in `traveller_world_detail.py` (2D + DMs from atmosphere, hydrographics, age, temperature; DM clamped to [-12,+4]; Special Cases 1 and 2 implemented). `_apply_biomass()` called as last step in `attach_detail()` to preserve RNG seed order for all prior fields. `WorldDetail` gains `biomass_rating: Optional[int]`. `World` gains `biomass_rating: Optional[int]` (set only when Mainworld Detail enabled). gen-ui: orbit Notes column shows 'Biomass N' when > 0; moon sub-row Notes column likewise; mainworld physical card always shows biomass rating. `traveller_world_schema.json` updated. Module-level `too-many-lines` suppression added to `traveller_world_detail.py` (now 1155 lines). 65 new tests; 1260 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-24 | Session 63 — Biocomplexity Rating implemented (WBH pp.127-131). New `generate_biocomplexity_rating()` in `traveller_world_detail.py` (2D−7 + min(biomass,9) + DMs; atmosphere not 4–9 DM−2; low-oxygen taint DM−2; age DMs using worst-at-boundary rule; min result 1). Helper `_set_biocomplexity()` extracted to module level. `_apply_biomass()` now also computes biocomplexity for all bodies with biomass > 0. `WorldDetail` gains `biocomplexity_rating: Optional[int]`. `World` gains `biocomplexity_rating: Optional[int]`. `BIOCOMPLEXITY_DESC` dict added to `tables.py`. System card orbit table gains "Biosphere" column showing "B, C" eHex for worlds with life. System card mainworld section gains "Native life" inner-card showing biomass + biocomplexity. World card Physical characteristics section shows biomass + biocomplexity when biomass > 0. Rare Earth Variant deferred to `context/deferred-features.md`. 1276 tests pass (no new tests needed); pylint 10.00/10 on all modified files including tables.py (pre-existing missing-module-docstring fixed). |
| 2026-05-24 | Session 64 — gen-ui radio toggle: replaced "System detail" + "Mainworld detail" checkboxes with a `QRadioButton` pair ("Mainworld only" / "Full detail") backed by a `QButtonGroup`. Full detail enables NHZ, oxygen, and map button; mainworld only disables and clears them. `_on_system_detail_toggled` renamed to `_on_detail_toggled`. |
| 2026-05-24 | Session 64 cont. — gen-ui two-tab display: `_show_system_summary()` now builds a `QTabWidget` (Tab 1 "System" = stellar + orbits cards in a `QScrollArea`; Tab 2 "Mainworld" = `QWebEngineView` showing `mw.to_html()`; default tab is Mainworld). `_build_system_summary_header()` return type changed from tuple to `QWidget` (orbit toggle checkbox removed; now superseded by tab switching). |
| 2026-05-24 | Session 64 cont. — Biocomplexity DM test coverage: added 41 new tests to `tests/test_biomass.py` covering every DM condition (`TestBiocomplexityAtmosphereDM`, `TestBiocomplexityLowOxygenDM`, `TestBiocomplexityAgeDM`, `TestBiocomplexitySpecialCases`). Schema gap fixed: `biocomplexity_rating` was missing from `traveller_world_schema.json` since Session 63. |
| 2026-05-24 | Session 64 cont. — GG satellite tidal stress (issue #74): `_apply_seismic_stress()` gains `gg_mass_earth: float = 0.0` and `gg_satellite_moon: Optional[Moon] = None` parameters. When the mainworld is a gas giant satellite with orbit data, a second `_compute_tidal_ss()` call adds the gas giant's tidal contribution; gas giant's tidal amplitude contribution also added to `tidal_amplitude_m`. Both `gen-ui/app.py` and `function_app.py` call sites updated. No new tests (covered by existing seismic integration tests). |
| 2026-05-24 | Session 64 cont. — Biomass temperature DM compliance fix: "High temperature" rows (DM-2 above 353K, DM-4 below 273K) were missing from `generate_biomass_rating()`. The WBH p.127 table has 5 temperature rows; the code only applied 3 (Mean temperature rows). Fix: when `mean_temp_k > 353` apply DM-2+DM-4 = DM-6; when `mean_temp_k < 273` apply DM-4+DM-2 = DM-6. Combined DMs now match the footnote simplified values (frozen/boiling → -6). 7 existing K-path tests updated (expected values changed); 3 new tests added (boundary at 353K/273K, footnote equivalence). 1320 tests pass; pylint 10.00/10. |
| 2026-05-24 | Session 64 cont. — Native sophont generation (WBH p.131). New `generate_sophont_checks(biocomplexity, age_gyr) -> tuple[bool, bool]` in `traveller_world_detail.py`. Current sophont: 2D + min(bio,9) − 7 ≥ 13; extinct: same + DM+1 if age > 5 Gyr (only when current fails). Triggered inside `_apply_biomass()` when biocomplexity ≥ 8. `World` gains `native_sophont: bool` and `extinct_sophont: bool` fields; both False by default; emitted to JSON only when True. World card biological detail section shows "Extant" or "Extinct (evidence present)". 11 new tests in `TestSophontChecks`. JSON schema gains `native_sophont`, `extinct_sophont`, and (fix) `biocomplexity_rating` properties. 1317 tests pass; pylint 10.00/10. |
| 2026-05-24 | Session 65 — Advanced mean temperature implemented (WBH pp.47-50). New `_roll_albedo()` (rocky/icy/icy-far base by density/hz_deviation; atmosphere thin/mid/very-dense/heavy modifiers; hydrographics modifier; clamped [0.02, 0.98]) and `_roll_greenhouse_factor()` (initial 0.5×√bar; standard/exotic/extreme atmosphere modifier) in `traveller_world_physical.py`. New public `generate_advanced_mean_temperature(physical, atmosphere, hydrographics, pressure_bar, luminosity, orbit_au, hz_deviation, orbit_eccentricity=0.0, star_mass=1.0)` sets five new `WorldPhysical` fields: `albedo`, `greenhouse_factor`, `advanced_mean_temperature_k`, `high_temperature_k`, `low_temperature_k`. Mean formula: T = 279 × ⁴√(L × (1-A) × (1+G) / AU²). High/Low use variance factors (Steps 1-9): `_axial_tilt_factor()` (sin of normalized tilt, orbital-year modifiers), `_rotation_factor()` (√|day|/50, capped 1.0, 1:1-lock → 1.0), `_geographic_factor()` ((10-HYD)/20), combined as luminosity modifier through atmospheric factor (1+bar), applied to high/low luminosity × near/far AU. BeltPhysical guard added. gen-ui: "Advanced temperature" checkbox (Full detail only); call site moved BEFORE `_attach_detail()` so advanced temps are on the physical object when `_apply_biomass()` runs. `generate_biomass_rating()` gains `high_temp_k` parameter: "High temperature" DM rows use `high_temp_k` (falls back to `mean_temp_k` proxy); "Mean temperature" rows use `mean_temp_k`; `_apply_biomass()` reads `advanced_mean_temperature_k` and `high_temperature_k` off the physical object via `getattr`. `templates/world_card.html` gains 5 new display rows. `traveller_world_schema.json` gains 5 new optional fields. 72 new tests in 6 new test classes; 1392 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-22 | Session 62 — Moon orbital eccentricity and inclination implemented (WBH p.76). `_roll_eccentricity` → `roll_eccentricity` and `_roll_inclination` → `roll_inclination` in `traveller_orbit_gen.py` (made public). `traveller_moon_gen.py` imports both; `Moon.orbit_retrograde: bool` replaced by `Moon.orbit_inclination: float` (retrograde implied by inclination > 90°). `generate_moons()` rolls both for each significant moon after orbit placement. Emitted to JSON as `orbit_eccentricity` (4 d.p.) and `orbit_inclination` (2 d.p.) when > 0. gen-ui Ecc/Incl column in moon sub-rows now shows `ecc/incl°`. Pyright fix: `age_gyr: float = system.stellar_system.primary.age_gyr or 0.0` in `_apply_biomass()` (was `float | None`). Test imports updated for renamed functions; 9 new tests in `TestMoonEccentricityInclination`; 1276 tests pass; pylint 10.00/10. |

| 2026-05-25 | Session 70 — gen-ui UX + build improvements. (1) Belt temperature fix (size 0): `BeltPhysical` gains `mean_temperature_k: int` computed via `_compute_mean_temperature(hz_deviation, 0)`; displayed in world_card.html, world_list.html, system_card.html, render_system_json.py belt branch; JSON schema updated; test_to_dict_keys updated. (2) TravellerMap async worker (issue #77): new `_TravMapWorker(QThread)` with `result`/`failed`/`ambiguous` signals; `_show_loading()` and `_start_travellermap_worker()` replace blocking `_do_travellermap_generation()`; Generate button disabled during lookup; disambiguation retry also uses async path. (3) QGroupBox checkbox hierarchy (issue #79): `_radio_mainworld_only`, `_radio_full_detail`, `_detail_group` removed; replaced with `self._system_group = QGroupBox("System detail", checkable=True)`; Qt auto-grays children when unchecked. (4) PyInstaller size reduction (issue #91): 37 Qt module excludes; optimize=2; strip=True; a.datas translation filter; a.binaries image-format filter; macOS CI split to macos-arm64 (macos-latest) + macos-x86_64 (macos-13) via TARGET_ARCH env var; UPX installed on all four CI platforms; release publishes 5 artifacts. 1449 tests pass. |
| 2026-05-27 | Session 82 — Habitability Rating implemented (WBH p.131, issue #106). New `generate_habitability_rating()` in `traveller_world_detail.py` (base 10 + DMs for size, atmosphere, hydrographics, tidal lock, temperature, gravity; min 0). `_atmosphere_habitability_dm()` and `_gravity_habitability_dm()` helpers; full/fallback temperature paths; worst-DM boundary rule for gravity. `habitability_description()` added to `tables.py`. `WorldDetail` gains `habitability_rating: Optional[int]`; `World` gains same field. `_apply_habitability()` called after `_apply_biomass()` in `attach_detail()`; secondary worlds use fallback path; mainworld uses full path when `WorldPhysical` available. `World.to_dict()`, `from_dict()`, `_world_html_ctx()` updated; `world_card.html` gains "Habitability" inner-card. JSON schema updated. 81 new tests in `tests/test_habitability.py`. 1644 tests pass; pylint 10.00/10. |
| 2026-05-28 | Session 85 — GG moons near the Roche limit (issue #120). Four related bugs fixed in the gas-giant satellite tidal-stress pipeline. (1) `_compute_tidal_ss` removed from the GG-satellite block in `_apply_seismic_stress()`: that formula is calibrated for AU-scale star→planet distances and produces ~10¹⁵× amplified results at moon distances (~0.1–0.8 Mkm); the `tidal_amp +=` line is retained. (2) TSF capped at 500: `tsf = min(math.floor(tidal_amp / 10), 500)`; beyond 500K-equivalent, liquefaction limits further tidal dissipation. (3) Perigee Roche limit check: after eccentricity/inclination roll, any significant moon whose `orbit_pd × (1 − e) < 2.0` is converted to ring material; `ring_count` incremented on existing ring or new ring created. (4) WBH GG mass roll: new `_roll_gg_mass(gg_category)` in `traveller_orbit_gen.py` (GS: 5×(1D+1)=10–35 M⊕; GM: 20×(3D−1)=40–340 M⊕; GL: D3×50×(3D+4)=350–3300 M⊕); result stored as `OrbitSlot.gg_mass_earth` (Optional[float], serialised via `to_dict`/`from_dict`); replaces `gg_diameter ** 2` at all five call sites with legacy-safe fallback (`orbit.gg_mass_earth if not None else float(diam ** 2)`). `_moons_for()` in `traveller_world_detail.py` gains `gg_mass_earth` parameter and passes it as `planet_mass_earth` to `generate_moons()`. Seed 253115564 (the reproducing case): TSF drops from 644 to 8, TSS from 1,256,530 to 0, total SS from 1,257,174 to 8. 142 new tests; 1657 tests pass; pylint 10.00/10 on all modified files (traveller_world_physical.py, traveller_moon_gen.py, traveller_orbit_gen.py, traveller_world_detail.py, function_app.py). |
| 2026-05-27 | Session 84 — Dark mode for gen-ui (issue #81). `gen-ui/app.py`: `QSettings` added to `QtCore` import; `_CSS_DARK` module-level stylesheet constant added (covers `QWidget`, `QGroupBox`, all named `QLabel#` and `QPushButton#` selectors); `AppWindow.__init__` reads `QSettings("traveller-world-gen", "AppWindow").value("dark_mode", False)` at startup and stores `self._dark_mode: bool`; `_apply_theme()` calls `app.setStyleSheet(_CSS_DARK if self._dark_mode else _CSS)`; `_themed_html(html)` injects `data-theme="dark"` attribute on `<html>` tag when dark mode is active; `_on_toggle_dark_mode(checked)` saves preference to `QSettings` and live-refreshes the displayed result; `_build_menu_bar()` adds a View menu with a checkable "Dark Mode" action. `templates/system_card.html` and `templates/world_card.html` each gain a `[data-theme=dark]` CSS block matching the respective custom-property naming conventions. HTML cards follow the in-app toggle rather than the OS `prefers-color-scheme` setting. No new tests; 1650 tests pass; pylint 10.00/10. |
| 2026-05-27 | Session 83 — Three issues closed. Issue #114: removed mainworld data from system card — the ~70-line `mw_data` construction block deleted from `TravellerSystem.to_html()`; corresponding ~200-line mainworld HTML section removed from `system_card.html`; eliminates duplication between System tab and Mainworld tab in gen-ui. Issue #115: MAO and HZ Orbit# columns added to Stars table in `system_card.html`; `star_rows` dict extended with `mao`, `hz_inner`, `hzco`, `hz_outer` fields from `SystemOrbits.star_mao/star_hzco/star_hz_inner/star_hz_outer`. Issue #116: ammonia fluid type bug fixed in `traveller_hydro_detail.py` — standard breathable atmospheres (0–9) at Cold temperature now correctly return Water instead of Ammonia; new `_AMMONIA_ELIGIBLE_ATMS = frozenset({10–15})` constant guards the Cold→Ammonia branch. Issue #112 confirmed already resolved (no code changes). 6 new tests in `test_hydro_detail.py`. 1650 tests pass; pylint 10.00/10. |
| 2026-05-27 | Session 81 — Basic mean temperature 1D+5 compliance fix (WBH p.47 footnote). `_compute_mean_temperature()` in `traveller_world_physical.py` now rolls 1D+5 instead of extrapolating when the result would be below 10K (modified roll ≤ -34). Updated docstring. Replaced `test_minimum_temperature_3k` with 3 new tests: 1D+5 triggered and die mapped correctly, all 6 die faces produce correct result, roll -33 threshold uses extrapolation (not 1D+5). 1563 tests pass; pylint 10.00/10. |
| 2026-05-27 | Session 80 — Tidal heating baked into advanced mean temperature. Removed `advanced_seismic_temperature_k` field from `WorldPhysical`; `_apply_seismic_stress()` now updates `advanced_mean_temperature_k`, `high_temperature_k`, and `low_temperature_k` in-place using ⁴√(T⁴ + TSS⁴) when TSS > 0 and the rounded value changes. JSON schema, `to_dict()`, `from_dict()`, and context docs updated. 5 existing `TestSeismicStress` tests replaced with equivalent in-place-update tests. 1561 tests pass; pylint 10.00/10. |
| 2026-05-27 | Session 79 — Two features: (1) `optional_inhospitable_rule` tests: 9 new tests in `TestOptionalInhospitableRule` in `tests/test_biomass.py` verifying the WBH p.130 single-2D / natural-12 group-roll rule (rule disabled path, non-12 zeroes all NHZ worlds, natural 12 winner/loser mechanics, moons follow their parent world, in-HZ worlds unaffected, no group roll when no NHZ worlds, `attach_detail` flag pass-through). (2) Tidal heating in advanced mean temperature: `WorldPhysical` gains `advanced_seismic_temperature_k: Optional[int]` field set by `_apply_seismic_stress()` as ⁴√(T_adv⁴ + TSS⁴) when `advanced_mean_temperature_k` is available and the adjustment differs. `to_dict()` / `from_dict()` updated; JSON schema updated. 5 new tests in `TestSeismicStress`. Pylint 10.00/10 on `traveller_world_physical.py`; 1561 tests pass. |
| 2026-05-27 | Session 78 — Issue #112 acceptance criteria: targeted tests for biological pipeline edge cases outside the habitable zone. WBH Suggested Usage confirmed: limiting full detail (biodiversity/compat/lifeform profile) to the mainworld is correct; secondary worlds receive only biomass + biocomplexity. Three new test classes in `tests/test_biomass.py`: `TestNHZAtmosphereCodes` (codes 16/17 verified for biomass clamping, biocomplexity DM-2, and compatibility DM-8); `TestExtremeHZDeviationWorlds` (frozen/boiling worlds with atm 0/1/16/17 always yield biomass 0 on both simplified-zone and K-temperature paths; seed sweeps); `TestAtmosphereDetailNoneGuard` (biologic/has_low_o/has_taint all default False when `atmosphere_detail=None`; integration test confirming `_apply_biomass` does not crash; integration test confirming biomass=0 short-circuit leaves downstream fields None). Also imported `_apply_biomass` at module level in `test_biomass.py`. 32 new tests; 1547 tests pass; pylint 10.00/10 on `traveller_world_detail.py`. |
| 2026-05-26 | Session 77 — System map world marker position. `_marker_xy()` in `system_map.py` changed from top-of-arc (`math.radians(half_deg)`) to one-third-down (`math.radians(half_deg / 3)`). `context/system-map.md` created (new context file for `system_map.py`). GitHub issue #112 filed: recheck biological detail calculations for worlds outside the habitable zone. No new tests; 1515 tests pass; pylint 10.00/10. |
| 2026-05-26 | Session 76 — Biodiversity, Compatibility, and Lifeform Profile (issue #104). `generate_biodiversity_rating(biomass, biocomplexity)` and `generate_compatibility_rating(biocomplexity, atm, age_gyr, has_taint)` added to `traveller_world_detail.py`; `_ATM_COMPAT_DM` and `_INHERENT_TAINTED_CODES` constants defined. `_apply_biomass()` extended to set `biodiversity_rating`, `compatibility_rating`, and `lifeform_profile` on mainworld after sophont checks. Three new `Optional` fields on `World`; `to_dict()`, `from_dict()`, `_world_html_ctx()` updated; `world_card.html` gets three new rows; JSON schema updated. `import math` added to `traveller_world_detail.py`. 17 new tests in `TestBiodiversityRating` and `TestCompatibilityRating`. 1515 tests pass; pylint 10.00/10. |
| 2026-05-26 | Session 75 — WorldDetail round-trip (issue #109). Moon.from_dict() in traveller_moon_gen.py: parses size string back to size_code via _EHEX.index(), restores all post-init orbit fields, uses local import of WorldDetail for nested moon detail. WorldDetail.from_dict() in traveller_world_detail.py: calls __init__ with constructor params, overrides trade_codes from saved value, restores BeltPhysical/WorldPhysical via "inner_au" key check, sets biomass_rating and biocomplexity_rating. OrbitSlot.from_dict() in traveller_orbit_gen.py: local import of WorldDetail and reconstruction when "detail" key present. 19 new tests across TestMoonFromDict, TestWorldDetailFromDict, TestOrbitDetailRoundtrip. 1498 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-26 | Session 74 — Bug fix #110: `World.from_dict()` now reads `has_gas_giant` directly from the saved boolean instead of re-deriving it from `gas_giant_count > 0`. One-line fix on line 1774 of `traveller_world_gen.py`; backward-compatible fallback to `gas_giant_count > 0` when key is absent (old JSONs). 3 new tests in `TestWorldDetailRoundtrip`. 1479 tests pass; pylint 10.00/10. |
| 2026-05-26 | Session 73 — Open system JSON: TravellerSystem.from_dict() reconstruction chain (issue #108). Star.from_dict() + StarSystem.from_dict() in traveller_stellar_gen.py; OrbitSlot.from_dict() + SystemOrbits.from_dict(d, star_system) in traveller_orbit_gen.py; TravellerSystem.from_dict() in traveller_system_gen.py. gen-ui/app.py: TravellerSystem added to import; _load_system_from_json() sets state and renders without re-running generation; _on_open_json() replaces "not yet supported" dialog with actual reconstruction. OrbitSlot.detail stays None (WorldDetail reconstruction deferred); display uses detail_attached=False. 16 new tests in tests/test_system_roundtrip.py; 1466 tests pass. Pylint 10.00/10. |
| 2026-05-26 | Session 72 — File menu with Save As, Open JSON, and version tagging (issues #75, #107). `gen-ui/app.py`: `_build_menu_bar()` adds File > Open JSON… (always enabled) and File > Save As… (Ctrl+S, enabled after generation). `APP_VERSION = "1.4.0"` constant. JSON save injects `"_app_version"` key; open parses and version-checks it (`QMessageBox.critical` on mismatch). World JSONs reconstructed via `World.from_dict()` → `_finish_generation()`; system JSONs show "not yet supported" dialog. Save filter reduced to HTML + JSON (Text removed). `_build_action_buttons()`, `_write_html()`, `_open_in_browser()`, and `self._html_path` deleted. `import json` and `World` added to imports; `QComboBox` and `_FORMATS` removed. Both result headers cleaned of vsep + action-button widgets. Font consolidation (issue #80): three-tier px scale across all four HTML templates. System card horizontal expansion (issue #80 follow-up): `max-width` removed from `.card`, `white-space:nowrap` on `td`. Control layout two-row split (issue #82): `_build_controls()` rewritten as QVBoxLayout with Name+Generate row 1, Seed row 2. Pylint 10.00/10; 1450 tests pass. |
| 2026-05-25 | Session 69 — gen-ui UX improvements. (1) Orbit table horizontal scroll (issue #76): `system_card.html` orbit survey table now scrolls horizontally via `.table-scroll{overflow-x:auto}` wrapper div + `white-space:nowrap` on `th`; `Ecc/Incl` header shortened to `e/i` in both `system_card.html` and `system_detail.html`; pure template change, no Python. (2) TravellerMap panel show/hide (issue #78): `_build_source_row()` in `gen-ui/app.py` now stores `self._tm_panel` (grid) and `self._tm_vsep` (separator) as instance attributes; `_on_source_toggled()` calls `setVisible()` on both instead of `setEnabled()` on individual fields; panel hidden by default (Procedural is default). Issues #76 and #78 closed on GitHub. 1449 tests pass. |
| 2026-05-26 | Session 71 — Biologic taint enabled (WBH p.83, issue #28). `_BIOLOGIC_SUBTYPE_ROLLS` frozenset removed from `traveller_world_gen.py`; subtype rolls 4 and 9 added to `_TAINT_SUBTYPE_TABLE` as `("Biologic", "B")`; reroll guard `if raw_sub in _BIOLOGIC_SUBTYPE_ROLLS: continue` removed from `_roll_single_taint()`. The `has_biologic_taint=True` path in `generate_biomass_rating()` (already wired in Session 61) now fires when an atmosphere detail taint has `subtype == "Biologic"`. Tests: 2 old `test_biologic_never_produced` tests deleted; 3 new tests added (`test_biologic_produced_on_roll_4`, `test_biologic_produced_on_roll_9`, `test_biologic_taint_enforces_biomass_floor`). Net +1 test; 1450 tests pass. `context/deferred-features.md` atmosphere taints row updated to Complete. `v1.4-new-features.txt` created. GitHub issues #91 (binary size) closed; #95–#103 (v1.5.0 social characteristics) and #104–#106 (v1.4.0 physical detail) filed. |
| 2026-05-25 | Session 67 — Hydrographic composition Phase 2 implemented (WBH pp.91-92). `HydrographicDetail` gains `fluid_type: Optional[str]` field determined by temperature zone (Boiling→Sulfuric Acid, Hot/Temperate→Water, Cold→Ammonia, Frozen→Liquid Hydrocarbons). New `_FLUID_TYPE_BY_TEMP` lookup dict, `_NO_SURFACE_LIQUID_ATMS` frozenset, and `_fluid_type(atmosphere, temperature)` pure helper. `generate_hydrographic_detail()` gains keyword-only `atmosphere: int = 0` and `temperature: str = "Temperate"` parameters; `fluid_type` is None for desert worlds (hydro 0) and gas-atmosphere worlds (codes 16/17). `to_dict()` emits `fluid_type` when set. 8 call sites updated across `traveller_system_gen.py`, `function_app.py` (×4), `traveller_map_fetch.py`, `gen-ui/app.py` (×2). Display: "Fluid type" row added after "Surface liquid" in `world_card.html`, `system_card.html`, `world_list.html`, `system_detail.html`; `render_system_json.py._mw_ctx()` gains `hyd_fluid_type`; `World.summary()` gains fluid type line. JSON schema updated with `fluid_type` optional string property. `context/deferred-features.md` "World physical detail" row updated to Complete. 20 new tests in `TestFluidTypeHelper` and `TestFluidTypeIntegration`; 1425 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-25 | Session 66 — Stellar day vs sidereal day implemented (WBH p.106, issue #20). New `_compute_stellar_day(sidereal_h, orbital_period_h, tidal_status)` pure helper in `traveller_world_physical.py`: prograde formula `(T_sid×T_orb)/(T_orb−T_sid)`; retrograde formula `(T_sid×T_orb)/(T_orb+T_sid)`; returns None for 1:1_lock and prograde edge cases where T_sid≥T_orb. `WorldPhysical` gains `stellar_day_hours: Optional[float]` field; set in `generate_world_physical()` when orbital data is present and recomputed in `apply_moon_tidal_effects()` (reuses existing `period_h` variable). `to_dict()` emits `stellar_day_hours` when not None. Display: "Stellar day" row added after "Day length" in `world_card.html`, `system_card.html`, `world_list.html`, `render_system_json.py._phys_rows()`, and `World.summary()` text output. 13 new tests (8 direct unit tests of `_compute_stellar_day` covering every branch + edge case; 5 integration tests through `generate_world_physical()`); 1405 tests pass; no new RNG calls. |
| 2026-05-28 | Session 86 — Injectable RNG (issue #42). Replaced global `random` module usage with `random.Random` instances injected at entry points and propagated through the pipeline. Each of the 8 generation modules (`traveller_hydro_detail.py`, `traveller_belt_physical.py`, `traveller_world_physical.py`, `traveller_world_gen.py`, `traveller_stellar_gen.py`, `traveller_orbit_gen.py`, `traveller_moon_gen.py`, `traveller_world_detail.py`) gained a module-level `_rng: random.Random = random` sentinel and all `random.XXX()` calls were replaced with `_rng.XXX()`. Public entry-point functions gained `rng: Optional[random.Random] = None`; when provided, they write it to `_rng`. `generate_world()` only replaces `_rng` when an explicit `seed` or `rng` is given (preserving global-seed behaviour for tests). `roll_eccentricity()` and `roll_inclination()` in `traveller_orbit_gen.py` take explicit `rng` for cross-module propagation from `traveller_moon_gen.py`. `TravellerSystem` gains `seed: Optional[int]` field; emitted by `to_dict()`, restored by `from_dict()`. `World` gains `seed: Optional[int]` field similarly. `apply_seed()` in `shared/helpers.py` now returns `Tuple[int, random.Random]` instead of just `int`; `random.seed()` call removed. `function_app.py`: all 13 handler call sites updated to `seed, rng = apply_seed(seed)` and pass `seed=seed, rng=rng` to generators; all manual `d["seed"] = seed` injections removed. `traveller_map_fetch.py`: `random.seed(seed)` replaced with `rng = random.Random(seed)` and `rng` propagated to `generate_orbits()`, `generate_hydrographic_detail()`, and `attach_detail()`. CLI `__main__` blocks updated to use `random.Random(seed)` and pass to generation functions. `conftest.py` gains an `autouse` fixture that resets every module's `_rng` to the global `random` module before and after each test, preventing order-dependent failures when tests use `random.seed()`. Three tests updated: `test_seed_produces_reproducible_world` uses `generate_world(seed=99)` twice; `test_nhz_off_secondary_deterministic` passes explicit `rng` to both `generate_full_system` and `attach_detail`; `test_batch_seed_matches_sequential_singles` uses `random.Random(seed)` and passes `rng` explicitly. 0 new tests; 1657 tests pass; pylint 10.00/10 on all modified files. |
| 2026-05-29 | Session 87 — Issues #118 (moon ecc/incl in orbit table), #119 (GG mass/density in Notes column), #84 (gen-ui onboarding card), #105 (terrestrial resource rating: `WorldPhysical.resource_rating`, density DM, biological DMs in `attach_detail()`), #122 (NHZ atmospheres API: `parse_nhz_atmospheres()`, `generate_system_from_world/map()` gain `nhz_atmospheres` param, wired into all system handlers, schema gains `$defs.system_generation_options`). Schema version bumped to v1.4.1. 1686 tests pass. |
| 2026-05-30 | Session 88 — Issue #113 (larger worlds for non-mainworld terrestrial bodies). Removed `min(size, 9)` cap from `generate_atmosphere()` call in `_terrestrial_sah()` and `_moon_detail()` in `traveller_world_detail.py`. Replaced with `min(generate_atmosphere(size), 15)` so sizes 10-15 (eHex A-F) use the full WBH `2D-7+Size` formula while capping at code 15 (F — Unusual) to exclude NHZ-only codes 16-17. SAH encoding and WorldDetail.profile parsing already handled eHex sizes correctly. 10 new tests in `TestLargeSecondaryWorldAtmosphere`; 1699 tests pass; pylint 10.00/10. |
| 2026-06-02 | Session 90 — FastAPI server (`fastapi/`). New `fastapi/app.py` (all 11 endpoints, SlowAPI per-IP rate limiting, no auth) and `fastapi/helpers.py` (flat module; FastAPI-native equivalents of every `azure-api/shared/helpers.py` function). `conftest.py` updated to add `fastapi/` to `sys.path`; `pyrightconfig.json` adds `"fastapi"` to `extraPaths`; `.github/workflows/dependency-audit.yml` audits `fastapi/requirements.txt`. 130 new tests in `tests/test_fastapi_app.py` (TestClient, no stubs); 1836 tests pass; pylint 10.00/10. |
| 2026-06-03 | Session 97 — Issue #128 (settlement type population modifiers). `_SETTLEMENT_DMS` and `_SETTLEMENT_DEFAULT_DM` module-level dicts; `_population_settlement_dm(settlement_type, atmosphere) → int`; `generate_population(settlement_dm=0)` gains DM param (result clamped to 0–10); `generate_world()` and `apply_mainworld_social()` gain `settlement_type: str = "standard"`. Five types: Standard (DM 0), Long-settled (+1/+2/+3 by atm), Well-settled (−1/+1/+2), Backwater (−5/−3/−1/+1), Unsettled (−7/−5/−4). Gen-ui `_OptionsDialog` gains `QGroupBox("Settlement type")` with 5 `QRadioButton`s; Standard is default; persisted as `opt_settlement_type` in QSettings; not applied on TravellerMap paths. 22 new tests in `TestSettlementType`; 1906 tests pass; pylint 10.00/10. |
| 2026-06-03 | Session 96 — FastAPI web UI split: `index.html` rewritten as "Mainworld Only" (single page, minimal generation matching gen-ui no-options output); new `fastapi/static/system.html` for "Full System" with tabbed Mainworld/System result. `/api/world/{name}/card` gains `detail` parameter (FastAPI): `detail=false` uses minimal pipeline (atmosphere + hydro only); `detail=true` generates full system + `attach_detail()`. `_OptionsDialog` in `gen-ui/app.py`: checkable `QGroupBox("System detail")` replaced with plain `QCheckBox` + indented `QWidget`; sub-options now hidden (not just disabled) when unchecked; `QGroupBox` import and `_CSS_DARK` rule removed. `.gitignore` updated to ignore `context/`, `fetch-context.sh`, `push-context.sh`. |
| 2026-06-03 | Session 95 — Issue #95 (population detail, WBH Social Characteristics). New `traveller_world_population_detail.py` with `City`, `PopulationDetail` dataclasses and `generate_pcr()`, `generate_urbanisation_pct()`, `generate_population_detail()`, `attach_population_detail()`. PCR 0–9 via 1D+DMs; force-9 short-circuit when 1D > pop_code (pop < 6). Urbanisation % via 2D+DMs → range table with inner dice; min/max constraints for pop 9/A+ and various TLs. Major cities via 5-case dispatch; Case 5 formula `ceil(2D−PCR+urb×20/PCR)`; chunk allocation algorithm for 4+ cities. Population profile `{pop}-{p}-{pcr}-{urb%}-{cities}`. `World.population_detail` and `WorldDetail.population_detail` fields added (to_dict, from_dict). New Population detail card in world_card.html. Gen-ui gains standalone "Population detail" checkbox. Schema updated (`population_detail` object, 11 sub-fields); APP_VERSION bumped 1.4.1 → 1.5.0. 23 new tests; 1884 total. |
| 2026-06-03 | Session 94 — Issue #18 (secondary world classifications, WBH p.163). New `_secondary_classification()` checks Colony/Farming/Freeport/Military Base/Mining Facility/Penal Colony/Research Base eligibility and rolls in table order. New `_apply_classification()` sets `WorldDetail.classification` and appends the two-letter code to `trade_codes`. `WorldDetail` gains `classification: Optional[str]` slot; emitted in JSON only when not None; restored by `from_dict()`. Called in `generate_system_detail()` (belt + terrestrial paths), `_moon_detail()`, and `apply_secondary_social()` (including `_moons_social()`). `system_body_table()` appends `[Classification Name]` to orbit and moon rows. Belt threshold for Mining Facility is 6+ instead of 10+. 14 new tests in `TestSecondaryWorldClassification`; 1861 tests pass; pylint 10.00/10. |
| 2026-06-04 | Session 99 — Issue #96 (government detail, WBH Social Characteristics §3). New `traveller_world_government_detail.py` with `Faction`, `GovernmentDetail` dataclasses and `generate_centralisation()`, `generate_authority()`, `generate_factions()`, `generate_government_detail()`, `attach_government_detail()`. Centralisation (Confederal/Federal/Unitary) via 2D+DMs; authority (Legislative/Executive/Judicial/Balanced) via 2D+DMs; structure via special per-gov rules then Functional Structure table. Balanced authority rolls all three branch structures separately. Factions via D3+DM count; each faction rolls gov type (2D−7+pop), strength (2D), and relationship to ruling body (1D+DM). Profile strings: `G-CAS` (non-balanced) and `G-CB-LS-ES-JS` (balanced). `World.government_detail` and `WorldDetail.government_detail` fields added (to_dict, from_dict). Government detail card in world_card.html. FastAPI: `parse_government_detail()` + `attach_government_detail()` added to 4 endpoints. Returns None for gov 0; gov 7 (Balkanised) deferred to issue #130. 24 new tests; 1930 total; pylint 10.00/10 on all changed files. |
| 2026-06-03 | Session 98 — FastAPI web UI parity with gen-ui. (1) `fastapi/helpers.py`: `parse_settlement_type()` validates against 5 keys (standard/long_settled/well_settled/backwater/unsettled), defaults to "standard"; `parse_population_detail()` bool flag. (2) `fastapi/app.py`: `attach_population_detail` imported; `_run_select_mainworld()` gains `settlement_type` param and passes it to `apply_mainworld_social()`; endpoints 3/5/7/8 now parse and apply `settlement_type` and `population_detail`; `logging.config.dictConfig` adds timestamps to all console output (uvicorn access/error and app loggers). (3) `fastapi/static/system.html`: controls restructured into primary row + sub-row; sub-row adds NHZ Atm, Biomass Rule, Runaway GH, Indep Gov (disabled when Detail/Full off), Pop Detail, and Settlement Type dropdown; save buttons (HTML/Text/JSON) appear in seed badge row after generation. (4) `fastapi/static/index.html`: save buttons (HTML/JSON) in seed badge row. 1906 tests pass; pylint 10.00/10. |
| 2026-06-02 | Session 93 — FastAPI web UI (`fastapi/static/index.html`) served at `/`; two-panel page (Mainworld + System) with card/JSON/text output, seed badge, server status indicator. Gen-ui: suppress macOS Chromium `TASK_CATEGORY_POLICY` stderr noise via `QTWEBENGINE_CHROMIUM_FLAGS=--log-level=3` before `QApplication`. Four CLI execution flowcharts added: `docs/traveller_world_gen_flowchart.md`, `traveller_system_gen_flowchart.md`, `traveller_map_fetch_flowchart.md`, `system_map_flowchart.md`. 1847 tests pass. |
| 2026-06-02 | Session 92 (cont.) — Re-enable secondary social generation after mainworld selection. New `apply_secondary_social(system, independent_government, rng)` in `traveller_world_detail.py` re-rolls the population cap and re-applies population, government, law, TL, spaceport, and trade codes to all secondary orbit and moon WorldDetail objects using the mainworld's real (post-selection) social values. Also syncs the satellite WorldDetail (built with placeholder UWP by `attach_detail()`) back to the mainworld's real social data. `_run_select_mainworld()` in both API layers gains `independent_government` parameter and calls `apply_secondary_social()` when `want_detail=True`. gen-ui calls `apply_secondary_social()` after `apply_mainworld_social()`. 1847 tests pass; pylint 10.00/10. |
| 2026-06-02 | Session 92 — Issue #125 (mainworld selection, WBH pp.155-156). `select_mainworld(system, rng)` in `traveller_system_gen.py` scores all terrestrial candidates (habitability×50, native_sophont×50, resource_rating×30, refuelling×10) and promotes the winner; 3D=18 triggers random selection. Swap logic: winning secondary regenerated via `generate_mainworld_at_orbit()`, old mainworld demoted to `WorldDetail`. `WorldDetail` gains `native_sophont: bool` field populated in `_set_biocomplexity()` (also fixes call order for `generate_sophont_checks` on secondaries). Wired into 4 azure-api system endpoints, 3 fastapi system endpoints, and gen-ui `_OptionsDialog`. FastAPI `index.html` gains "Select MW" checkbox. 4 `test_biomass.py` tests updated for extra sophont dice. 11 new tests. 1847 tests pass; pylint 10.00/10 on all changed files. |
| 2026-06-02 | Session 91 — Issue #124 (defer social/starport/TL/bases/zone generation to after mainworld selection). `generate_mainworld_at_orbit()` in `traveller_system_gen.py` now returns a physical-only world (SAH + atmosphere/hydrographic detail + gas giant/belt counts); social steps 5–10 and 12–13 removed. New `apply_mainworld_social(world, rng=None)` in `traveller_world_gen.py` performs the deferred steps and is to be called after mainworld selection. Nine social imports removed from `traveller_system_gen.py`. `test_generate_system_detail_propagates_flag` updated to call `apply_mainworld_social()` before `generate_system_detail()` so secondaries can be inhabited. Also fixes runaway greenhouse bug: `atmosphere_detail` and `generate_gas_mix` now regenerated for the new A/B/C code after a greenhouse conversion in `function_app.py` and `gen-ui/app.py`. 1836 tests pass; pylint 10.00/10 on changed files. |
| 2026-05-30 | Session 89 — Issue #17 (secondary world independent government, WBH p.162). Added `_independent_government(population)` → `max(0, 2D-7+Pop)`. Updated `_secondary_law_level()` with `independent: bool = False` to bypass the captive-table path for Case 2. `WorldDetail` gains `is_independent_government: bool` slot; emitted in `to_dict()` only when `True`; restored by `from_dict()`. `_moon_detail()`, `generate_system_detail()`, and `attach_detail()` all gain `independent_government: bool = False` parameter threaded through all inhabited-world call sites (belt, terrestrial, and moon). `_moon_detail` pylint suppression extended to include `too-many-branches`. `_OptionsDialog` in `gen-ui/app.py` gains `independent_government` checkbox (fifth sub-option under System detail group); `AppWindow` gains `_opt_independent_gov` persisted in QSettings and passed to `_attach_detail()`. 7 new tests in `TestIndependentGovernment`; 1706 tests pass; pylint 10.00/10. |
