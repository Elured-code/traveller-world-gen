# ── Python ─────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ────────────────────────────────────────────────────
.venv/

# ── Testing ─────────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
.hypothesis/

# ── Azure Functions ─────────────────────────────────────────────────────────
# local.settings.json contains secrets (storage connection strings, keys).
# A safe template is provided as local.settings.json.example — copy and fill in.
local.settings.json
# Azure Functions publish artefacts
.python_packages/
.funcignore

# Generator package installed into azure-api/ by scripts/prepare_azure.sh via
# `pip install --target`.  Not committed — these are deployment artefacts.
azure-api/fastapi/
azure-api/traveller_gen/
azure-api/traveller_gen-*.dist-info/

# Auto-generated version file — run scripts/compute_version.sh to create
src/traveller_gen/_version.py

# ── Editors ─────────────────────────────────────────────────────────────────

.idea/
*.swp
*.swo
*~

# ── OS ──────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Install-generated launchers ─────────────────────────────────────────────
# Created by install.sh / install.ps1 — user-specific, not committed.

run-world.sh
run-world.bat
run-mapfetch.sh
run-mapfetch.bat


# ── Generated outputs ───────────────────────────────────────────────────────
# The examples/ directory contains generated HTML cards for illustration.
# Regenerate with: traveller-world --name <name> --html
examples/*.html

# ── Scratch / working notes ─────────────────────────────────────────────────
v13.md

# ── Claude Code context ─────────────────────────────────────────────────────
# Project context and session history — internal development notes not
# intended for the public repository.
context/
.context/
fetch-context.sh
push-context.sh
.claude/settings.local.json
.claude/settings.json
